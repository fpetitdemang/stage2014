\documentclass[french]{book}
\usepackage[T1]{fontenc}
 \usepackage[french]{babel}
\usepackage[latin9]{inputenc}
\usepackage{geometry}
\geometry{verbose}
\usepackage{color}
\usepackage{float}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{glossaries}
\usepackage[toc,page]{appendix}
\usepackage{wrapfig}
\usepackage{comment}
\usepackage{natbib}
\usepackage{lscape}

\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}


%\documentclass{minimal}
\usepackage{tikz}
\usetikzlibrary{trees}
\usetikzlibrary{positioning,shadows,arrows}

\usepackage{hyperref}
\hypersetup{%
colorlinks,%
citecolor=black,%
filecolor=black,%
linkcolor=black,%
urlcolor=black%
}


\makeglossaries


\usepackage[babel=true]{csquotes}

\makeatletter
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Textclass specific LaTeX commands.
 \theoremstyle{definition}
 \newtheorem*{defn*}{\protect\definitionname}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% User specified LaTeX commands.
\usepackage{graphicx}
\usepackage{listings}
\usepackage{framed}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Pour introduire des commentaires

\newcommand{\nb}[2]{
    \fbox{\bfseries\sffamily\scriptsize#1}
    {\sf\small\textit{#2}}
}

\newcommand\cn[1]{\nb{MM}{#1}}
\newcommand\mh[1]{\nb{MH}{#1}}
\newcommand\mm[1]{\nb{MM}{#1}}
\newcommand\yb[1]{\nb{YB}{#1}}
\newcommand\xd[1]{\nb{YB}{#1}}
\newcommand\fp[1]{\nb{FP}{#1}}

\makeatother

\usepackage{babel}
\usepackage{fancyheadings}
\providecommand{\definitionname}{Definition}



\begin{document}
%\pagenumbering{roman}
\pagestyle{fancyplain}
\thispagestyle{empty}
\noindent
\begin{center}
\large{\texttt{Académie de Montpellier}}\\
\Large{\texttt{Université Montpellier II}}\\
\large{\texttt{Sciences et Techniques du Languedoc}}\\
\end{center}

\vspace{1cm}

\begin{center}
\Huge{{MÉMOIRE DE\\}}
 \vspace{1.0cm}
\Huge{\textbf{STAGE RECHERCHE DE MASTER M2 INFORMATIQUE}}
\begin{center}
\vspace{1.0cm}

\normalsize
effectué au Laboratoire d'Informatique de Robotique\\
et de Micro-électronique de Montpellier
\end{center}

\vspace{2mm}
%\Large{\textbf{prénom NOM}}

\vspace{0.1cm}
\normalsize

\vspace{3mm}

\large{Spécialité} : \textbf{AIGLE}\\
%\Large{Formation Doctorale} : \textbf{Informatique}\\
%\large{{\'E}cole Doctorale} : \textbf{Information, Structures, Systèmes}
\vspace{1.0cm}

\LARGE{\textbf{Inférence de la structure d'une page web en vue d'améliorer son accessibilité}}
\vspace{2mm}

\begin{center}
  par \textbf{Franck~PETITDEMANGE}
\end{center}

\vspace{2mm}



\vspace{2cm}

Date : \textbf{27 juin 2014}

\vspace{0.5cm}

Sous la direction de \textbf{Marianne~HUCHARD, Michel~MEYNARD, Yoann~BONAVERO}

\vspace{5mm}

%\begin{table}[hbt]
%\begin{center}
%\begin{tabular}{ l l l}
%Messieurs & {\em } & Rapporteurs\\
%& {\em } & \\
%& {\em } & \\
%\\
%Messieurs& {\em } & Examinateurs \\
%& {\em } & \\
%& {\em } & \\
%\\
%Monsieur & {\em Evripidis BAMPIS} &  Directeur de thèse\\
%& {\em  X} & \\
%\end{tabular}
%\end{center}
%\end{table}

%\vspace{.7cm}


\end{center}


\newpage\thispagestyle{empty}
\strut
\newpage\thispagestyle{empty}
\begin{center}
\huge Remerciements
\end{center}
%Mme Marianne Huchard
%M. Michel Meynard 
%M. Bonavero Yoann
%Mme Annie Chateau
\newpage\thispagestyle{empty}

\newpage
\tableofcontents
\newpage

\newpage
\listoffigures
\printglossaries
\glsaddall
\makeglossaries
\newglossaryentry{ontologie}
{
  name=Ontologie,
  description={En philosophie, l'ontologie est l'étude de l'être en tant qu'être, c'est-à-dire l'étude des propriétés générales de ce qui existe. Par analogie, le terme est repris en informatique et en science de l'information, où une ontologie est l'ensemble structuré des termes et concepts représentant le sens d'un champ d'informations \emph{wikipedia}},
  plural=ontologies
}

\newglossaryentry{DOM}
{
  name=Document Object Model,
  description={Le Document Object Model (ou DOM) est un standard du W3C qui décrit une interface indépendante de tout langage de programmation et de toute plate-forme, permettant à des programmes informatiques et à des scripts d'accéder ou de mettre à jour le contenu, la structure ou le style de documents XML et HTML \emph{wikipedia}}
}

\newglossaryentry{W3C}
{
 name=W3C,
 description={Le World Wide Web Consortium, abrégé par le sigle W3C, est un organisme de normalisation à but non lucratif, fondé en octobre 1994 chargé de promouvoir la compatibilité des technologies du World Wide Web telles que HTML}
}

\chapter{Introduction}
Le World Wide Web (www) est un réseau de ressources. Leur publication repose sur un langage universellement compréhensible et accepté par tous les ordinateurs : HTML. Historiquement conçu pour faciliter l'échange d'articles dans la communauté scientifique,  la démocratisation du web a fait radicalement évoluer le contenu d'une page web, sans pour autant que le langage ne suive cette évolution. 
Le contenu des pages web est passé d'un contenu homogène, une page contenant un simple texte structuré, à un contenu hétérogène. De nos jours, les pages sont constituées de différentes structures organisant différentes informations. Les différentes structures peuvent être la bannière de la page qui renseigne sur l'identité du site, un menu de navigation qui présente l'organisation du site web, ou un article qui caractérise l'information principale. 



La version 4  de HTML est le langage de publication le plus utilisé actuellement. Ce langage est complètement inadapté à la description d'un contenu hétérogène. Il n'y a aucune sémantique associée aux éléments de langage pour décrire les structures qui organisent les différentes informations dans une page : on ne peut pas annoter une structure comme étant un menu de navigation. De plus HTML 4 possède une composante syntaxique extrêmement souple permettant la construction d'un nombre virtuellement infini de construction pour décrire le contenu d'une page. 
%Il n'y a pas de pattern standard pour construire les différents objets constituant la page HTML 4 est un langage qui permet une grande souplesse syntaxique qui permet de la pas tenir compte des spécification de rédactions de page web par la W3C. râce à la grande souplesse syntaxique du langage, conçoivent diverses structures pour décrire une information. Les structures ne respectent aucun schéma standard. L'analyse du contenu de la page est devenu impossible puisque chaque concepteur suit son propre schéma de conception. 

La compréhension du contenu par un processus automatiques s'est ainsi complexifiée. Ce manque de compréhension du contenu des pages impacte par exemple les processus d'indexation des moteurs de recherche. Dans le contexte du stage on s'intéresse particulièrement à la compréhension du contenu pour les outils d'accessibilité destinés aux personnes en situation de handicap visuel.

La question de recherche posée par le stage s'intéresse au moyen d'inférer, par un processus automatique, les différentes informations structurant une page web.
%expliquer le sujet par rapport au travaux de yoann
Pour cela nous étudions dans un premier temps l'environnement des pages web afin de conceptualiser les différents éléments constitutifs de celles-ci. Cette étude se concrétise par la réalisation d'un méta-modèle de page web. Nous validons ce méta-modèle en évaluant son expressivité par la modélisation des préférences d'utilisateurs sur l'accessibilité des pages web.
Une fois cette étape achevée nous élaborons une méthode d'extraction des différents éléments constitutifs d'une page web. Cette méthode est complétée par un processus d'annotation de ces éléments d'après notre méta-modèle. La validation de l'approche consiste à vérifier la conformité des éléments extraits et leurs annotations d'après les spécifications du méta-modèle construit. 

La grande souplesse syntaxique de HTML 4 amène deux problèmes. Le premier est une diversité de structure dans la représentation des informations d'une page (\emph{e.g} on peut représenter un menu de dix façons différentes). Ce premier point écarte une approche basée sur la reconnaissance de pattern. Le second problème de la syntaxe souple est qu'il est possible de construire une page qui ne respectent pas les spécifications de la \gls{W3C}. Ce point implique ne peut pas effectuer un découpage du DOM, pour extraire les différents contenus, seulement en respectant la sémantique associée aux étiquettes HTML (d'après la norme de la \gls{W3C}). D'autre part, la construction des pages web repose également sur un langage pour décrire la mise en forme. Le problème est un possible écart entre la structure du DOM et la structure de la page au moment de l'affichage dans le navigateur. %Le problème est que la structure de la page décrite avec HTML peut être divergeant par rapport à la structure finale. 

Pour définir notre méthodologie, le constat, qui oriente la direction de nos travaux, est que la structure d'une page est explicitée au moment de la mise en page (par la navigateur), c'est-à-dire par l'application des différents styles de mise en forme associés aux balises HTML. Nous pensons donc qu'il est possible d'exploiter la mise en forme associée aux éléments de langage pour isoler les groupes qui structurent une information. Nous employons par la suite le terme de structures logiques pour désigner un ensemble d'éléments qui mis en relation remplissent une fonction (\emph{e.g} un menu, une barre de recherche). 

Ce rapport se structure comme suit. La chapitre \ref{chap:accessibilite} présente des préférences d'utilisateurs sur l'accessibilité. Ces préférences sont extraites de travaux réalisés antérieurement à ce stage.
Le chapitre \ref{chap:etat} présente un état de l'art. Il introduit tout d'abord une synthèse des langages de publication  que nous avons réalisée à partir des spécifications des différents standards. Nous y présentons également des approches existantes permettant d'isoler les différentes structures logiques ainsi que des solutions pouvant leur associer une sémantique. 
Dans le chapitre \ref{chap:realisation} nous présentons la contribution de notre stage. Dans la première partie du chapitre nous présentons le méta-modèle de page web créé. Dans la seconde partie du chapitre, nous proposons l'adaptation d'une méthode pour identifier la structure d'une page. Cette approche fonctionne sur la base d'un algorithme de segmentation basé sur des heuristiques que nous avons développées. Cette méthode est complétée par un processus d'annotation des structures extraites de la phase précédente. Le chapitre \ref{chap:resultat} présente et analyse les premiers résultats et une discussion de ceux-ci. Pour finir le chapitre \ref{chap:conclusion} présente une synthèse des travaux effectués et leurs perspectives.

\chapter{Accessibilité et contexte du stage \label{chap:accessibilite}}
Ce stage s'inscrit dans le contexte d'une thèse en cours de réalisation sur l'accessibilité numérique. L'un des objectifs de la thèse est prendre en compte les préférences d'un utilisateur dans un processus d'adaptation de pages web. L'accessibilité désigne le caractère possible de la liberté de déplacement dans l'espace, d'utilisation d'outils, et de compréhension. Appliqué aux pages web l'accessibilité signifie la possibilité d'accéder aux informations contenu sur dans une page et d'interagir avec. Certain handicap peuvent brider son l'accessibilité. Les handicaps peuvent être visuelles, moteurs ou cognitives. La thèse se concentre sur les handicaps visuelles. Le constat de l'état de l'art réalisé (dans la thèse) met en évidence que les outils existant pour améliorer l'accessibilité d'une page ne prennent pas en compte le profil des utilisateurs. Les outils sont peu personnalisables et possèdent des fonctionnalités d'accessibilité très génériques. Ce point pose problème car certain type d'adaptation qui rend accessible un contenu pour une personne peut rendre ce même contenu inaccessible pour d'autres personnes. Cette configuration implique le besoin de prendre en compte les préférences d'un utilisateur.
%Pour un traitement automatique les expression de ces préférences ont besoin d'être modélisé. Nous L'orientation du stage s'intéresse en premier lieu aux handicaps visuelles. Cette catégorie regroupe un très grand nombre de pathologies comme la myopie ou la presbytie. Ces pathologies impactent la vision et la perception des contrastes et couleurs.
%Par exemple, , ainsi une mauvaise combinaison de couleur peuvent rendre un contenu illisible. Par exemple, la couleur d'un texte dont la couleur de fond est trop proche peut rendre le texte inaccessible. Les personnes âgées sont sujettes à des dégénérescences de la rétine ce qui entraîne une diminution de la vue. Ces catégories de personne deviennent plus sensibles à la taille des polices de caractère. Des caractères trop petits vont engendrer des difficultés de lecture.

La figure \ref{workflow-adaptation} présente le processus global d'adaptation de pages web. Ce processus d'adaptation est une proposition faite dans ce stage pour faciliter le traitement des préférences de l'utilisateur. Il s'agit d'un processus d'ingénierie dirigée par les modèles. L'idée est de s'intéresser à la conception des pages d'un point de vue particulier et plus abstrait. On ne manipule que les concepts qui nous intéressent pour modéliser les préférences utilisateurs. Le schéma présente des pages sources (pages web) conforme à un méta-modèle de HTML 4. Ce méta-modèle n'est pas un support adapté à l'expression de préférences, nous développons ce point de vu dans la section \ref{sec:meta-modele}. Le schéma décrit donc une première transformation d'une page source vers un modèle source conforme à un méta-modèle intermédiaire plus adapté à la modélisation de préférences. Une fois cette représentation plus abstraite construite une seconde étape de transformation intervient pour appliquer les préférences des utilisateurs. Une fois appliquée ces préférences une troisième transformation intervient pour générer une nouvelle page conforme aux préférences de personnalisation de l'utilisateur dans les outils d'accessibilité. Par rapport a ce schéma, notre le travail de ce stage se situe dans la réalisation du méta-modèle intermédiaire et dans la transformation d'une page web conforme à HTML 4 vers un modèle conforme au méta-modèle intermédiaire.

\begin{figure}[h]
\centering
\includegraphics[scale=0.5]{img/transformation_endogene_1.jpg}
\caption{Processus d'adaptation d'une page web \label{workflow-adaptation}}
\end{figure}

Ci-dessous nous présentons plusieurs types (non-exhaustives) de problèmes redondants dans l'accessibilité des pages web : 
\begin{description}
	\item[Problèmes de surcharge visuelle :] Des pathologies impliquant une bases vision nécessitent l'utilisation de fort zoom sur une page. Si la page web contient trop d'informations, la navigation dans le site devient difficile. En effet, le lecteur n'a accès qu'a un fragment de la page %une page qui contient trop d'élément demande beaucoup d'effort de concentration
	\item[Problèmes de police de caractère :] Les personnes âgées sont sujettes à des dégénérescences de la rétine ce qui entraîne une diminution de la vue. Ces catégories de personne deviennent plus sensibles à la taille des polices de caractère. Pour rendre accessible le texte, les outils d'accessibilités augmentent la taille des caractères. La trop grande différence de taille entre les caractères du corps d'un texte et les éléments de titre les rendent illisibles.
	\item[Problèmes de contrastes :] Des pathologies comme le daltonisme entraînent la perte de la perception des contrastes. Des couleurs trop proches entre un texte et son arrière plan peuvent rendre ce texte illisible. 
\end{description}

Le méta-modèle 


%D'après ces quelques éléments on peut en déduire certaine préférence qui par leur modélisation validerons notre méta-modèle. La surchage visuelle implique  




\chapter{État de l'art \label{chap:etat}}
\section{Langage de publication de page web \label{chap-langage}}
\subsection{HTML 4\label{sub:HTML-4}}

HTML 4 \cite{HTML_4} est un langage permettant la publication de
contenu sur le web. C'est le langage standard actuel des pages web.
Il permet de structurer le contenu et de lui associer une mise en
forme. Le contenu est un contenu multimédia (texte, images, \emph{etc.}) . Ce contenu est organisé de manière hiérarchique en
le découpant en section et sous-section. 


\paragraph{Contenu}

Le contenu principal décrit dans les pages HTML 4
est un contenu textuel. Il peut également contenir du multimédia comme
des images, des vidéos et applets (des programmes qui sont automatiquement
chargés puis lancés sur la machine de l'utilisateur). L'inclusion
de contenu multimédia se fait par l'élément générique : <object>. Il
possède une collection d'attributs prédéfinis qui décrivent l'objet inclus
dans la page. Le principal étant \emph{type }décrivant
le type de contenu des données (\emph{e.g.} figure \ref{balise_object}). La valeur de ces attributs n'est pas prédéfinie. Elle est interprétée 
librement par la machine qui charge la page web. 

\begin{figure}[H]
\lstset{
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.2\textwidth, xrightmargin=.2\textwidth
}
\begin{lstlisting}[frame=single, language=html,]
<object data="data/test.mpg" type="video/mpeg">
	Ceci est une vidéo
</object>
\end{lstlisting}
\caption{Exemple contenu multimédia\label{balise_object}}
\end{figure}


\paragraph{Structuration générique}

HTML 4 propose un mécanisme générique pour la composition de contenu
formant la structure des pages web. Ce mécanisme gravite autour des
éléments de type <DIV> et de leurs attributs respectifs : \emph{id} et \emph{class}.


\subparagraph{DIV}

Signifiant division, la balise DIV est utilisée comme conteneur générique, il peut
contenir n'importe quel élément. Il est exploité pour :
\begin{itemize}
\item regrouper les éléments pour leur appliquer un style (une mise en forme
particulière).
\item signaler une section ou une sous-section. 
\end{itemize}

\subparagraph{id et class \label{html4-classe}}

Chaque élément peut se voir attribuer un identifiant ou une classe
d'appartenance. \emph{id} assigne un nom à un élément. Ce nom est
unique dans le document. \emph{class} au contraire, assigne un ou
plusieurs noms de classe à un élément. Un nom de classe peut être
partagé par plusieurs instances d'éléments. Les identifiants et les
classes sont des suites de caractères quelconques décidées arbitrairement
par l'auteur du document.

~\\

Les éléments DIV utilisés conjointement avec les attributs \emph{id} et \emph{class}
sont au c\oe ur du mécanisme générique de structuration d'un document.
DIV permet de diviser le contenu d'un document en sections et sous-sections 
(\emph{e.g.} figure \ref{div_sectionnement}) pour décrire sa structure. Les
balises <div> ayant une sémantique neutre, c'est l'auteur du contenu
qui attribue (de manière arbitraire) un nom de \emph{class} ou un
\emph{id}. L'\emph{id}
ou la \emph{class} est associé à
une mise en forme définie a priori. La mise en forme est définie au
travers d'un langage : CSS\cite{CSS} que l'on appelle feuille de style.
CSS permet d'appliquer un ensemble de règles de style ou un agencement des éléments
dans l'espace de la page. Par exemple, l'auteur peut
déclarer une classe ``aside'' et définir que les éléments appartenant
à la classe ``aside'' doivent être placés sur le côté droit de la
page avec un fond blanc. Ce mécanisme est illustré par la figure \ref{div_role}. L'auteur associe à chaque <DIV> une 
\emph{class}
ou un \emph{id} auquel s'applique
une mise en page et une mise en forme définies par l'auteur dans une
feuille de style CSS.

\begin{figure}[htb]
\lstset{
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.25\textwidth, xrightmargin=.25\textwidth
}
\begin{lstlisting}[frame=single, language=html]
<body>
	<div id="header" ></div>
	<div id="navigation_bar"></div>
	<div class="aside"></div>
	<div class="section">
		<div class="article"></div>
		<div class="article"></div>
	</div>
	<div class="aside"></div>
	<div id="footer"></div>
</body>
\end{lstlisting}

\centering
\includegraphics[width=0.5\linewidth]{img/architecture_Geekos_HTML4-294x300.jpg}

\caption{Architecture page web HTML 4\label{div_role}}
\end{figure}


\begin{figure}[htb]
\lstset{
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.15\textwidth, xrightmargin=.15\textwidth
}
\begin{lstlisting}[frame=single, language=html]
<body>
<div class="section" id="elephants-foret" >
	<h1>Les éléphants des forêts</h1> 
	<p>Dans cette partie, nous abordons le sujet 
moins connu des éléphants des forêts.</p>
	<div class="sous-section" id="habitat-foret" > 
	<h2>L'habitat</h2> 
	<p>Les éléphants des forêts ne vivent pas 
dans les arbres mais au milieu d'eux.</p>
	</div> 
</div>
</body>
\end{lstlisting}

\caption{Exemple découpage en sections et sous-sections \label{div_sectionnement}}
\end{figure}








\subsection{HTML 5}

HTML 5 \citep{HTML_5} étend HTML 4 en apportant de nouveaux éléments
lexicaux. Ces nouveaux éléments apportent une sémantique standard et de plus haut niveau. Elle permet notamment d'expliciter la structure d'une page.


\paragraph{Contenu}

HTML 5 fournit de nouveaux éléments comme <video>,
<audio> avec un ensemble d'attributs propres à chaque balise (a contrario
de l'élément <object> de HTML 4). Les attributs spécifiques permettent
de renseigner l'état d'un élément. Par exemple, la balise <audio>
possède un attribut spécifique \emph{muted}
indiquant si le son de l'élément audio est coupé ou non.


\paragraph{Structuration}

Les nouveaux éléments de HTML 5 spécifient donc une sémantique standard. Ci-dessous la liste des principaux :
\begin{itemize}
\item SECTION : représente une section générique dans un document, c'est-à-dire un regroupement de contenu par thématique.
\item ARTICLE : représente un contenu autonome dans une page, facilite l'inclusion
de plusieurs sous-documents.
\item NAV : représente une section de liens vers d'autres pages ou des fragments
de cette page
\item ASIDE : représente une section de la page dont le contenu est indirectement
lié à ce qui l'entoure et qui pourrait être séparé de cet environnement
\item HEADER : représente un groupe d'introduction ou une aide à la navigation. Il forme l'entête d'un document.
Il peut contenir des éléments de titre, mais aussi d'autres éléments
tels qu'un logo, un formulaire de recherche, etc.
\item FOOTER : représente le pied de page, ou de la section, ou de la racine
de sectionnement la plus proche
\end{itemize}
La figure \ref{html5_role} montre un découpage explicite de la structure
avec HTML 5 en opposition au découpage implicite de HTML 4 montré dans
la figure \ref{div_role}.

\begin{figure}[htb]
\begin{center}
\lstset{ 
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.15\textwidth, xrightmargin=.15\textwidth
}
\begin{lstlisting}[frame=single, language=html]
<body>
	<header></header>
	<nav></nav>
	<section>
		<article></article>
		<article></article>
	</section>
	<aside></aside>
	<footer></footer>
</body>
\end{lstlisting}

\includegraphics[width=0.7\linewidth]{img/architecture_Geekos_HTML5-293x300.jpg}

\caption{Exemple d'attribution de rôle\label{html5_role}}
\end{center}
\end{figure}



\subsection{ARIA}

ARIA (Acessible Rich Internet Application) \cite{ARIA} est la spécification
d'une \gls{ontologie} décrivant une interface graphique. Elle fournit des
informations sur la structuration d'un document et plus généralement
elle décrit les éléments qui composent une interface graphique au moyen d'un ensemble
de rôles, d'états et de propriétés.


\paragraph{Rôle}

Les rôles permettent d'identifier la fonction de chaque élément d'une
interface. Ils sont regroupés en trois catégories :
\begin{itemize}
\item Widget Roles : définit un ensemble de widget (alertdialog,
button, slider, scrollbar, menu, etc.)
\item Document Structure Roles : décrit les structures qui organisent un
document (article, definition, entête, ect.)
\item Landmark Roles : décrit les régions principales d'une interface graphique
(main, navigation, search, etc.)
\end{itemize}

\paragraph{États et propriétés}

ARIA prend en compte l'aspect dynamique et interactif des éléments
d'une interface. Elle permet d'associer des états et des propriétés
aux éléments d'une interface. Un état est une configuration unique
d'un objet. Par exemple, on peut définir l'état d'un bouton par l'état
\emph{aria-checked }qui peut prendre trois propriétés suivant l'interaction
avec l'utilisateur : \emph{true - false - mixed} . Dans le cas d'une
checkbox, \emph{true} indique si la checkbox est cochée, \emph{false}
si elle ne l'est pas et \emph{mixed} dans le cas d'un ensemble de checkbox
indique que certaines sont cochées.

Aria prévoit même un système d'annotation pour les objets ayant des
comportements asynchrones. Par exemple, on peut indiquer par une annotation qu'un élément
se met à jour de manière autonome.

\begin{comment}
\paragraph{Méta-modèle}
La figure \ref{} modélise les principales méta-classe de la norme ARIA. On distingue deux groupes.
Un premier groupe pour la description des éléments d'interactions qui hérite de la méta-classe \textbf{Widget}  :
\begin{itemize}
\item La méta-classe \textbf{Composite} indique qu'un élément graphique possède des éléments navigables. Elle permet d'organiser la navigation à l' intérieure d'un \emph{Wigdet}.
\item  La méta-classe \textbf{Input} définit des éléments qui permettent des saisies de la part d'un utilisateur.
\item La méta-classe \textbf{Command} définit des éléments qui réalisent des actions. Par exemple, l'envoie de données vers un serveur.
\end{itemize}
Un deuxième groupe pour les éléments de structuration qui héritent de la méta-classe \textbf{Structure} 
\begin{itemize}
\item La méta-classe \textbf{Section} définit les éléments qui définissent une unité de confinement structurelle
dans une page.
\item La méta-classe \textbf{Sectionhead} définit les éléments qui introduisent des titres. 
\item La méta-classe \textbf{Landmark} définit les principaux points d?intérêts dans une page. Par exemple, la banniere d'une page, les formulaires, le contenu principal.
\end{itemize}
\end{comment}

\subsection{CSS }

CSS est un langage de feuille de style qui permet aux auteurs des
pages web de lier du style aux éléments HTML. Le style définit comment
afficher un élément (ex. les polices de caractères, l'espacement,
couleurs, \emph{etc.}). CSS permet ainsi de séparer la présentation
du style du contenu (\emph{cf. }figure \ref{css-font}). L'avantage
est une simplification de l'édition et de la maintenance d'une page
web.

\begin{figure}[H]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.1\textwidth, 
	xrightmargin=.1\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<style>
p.serif{font-family:"Times New Roman",Times,serif;} 
p.sansserif{font-family:Arial,Helvetica,sans-serif;} 
</style> 

<body> 
<p class="serif">Ceci est un paragraphe 
avec un style de font Times New Roman font.</p> 
<p class="sansserif">Ceci est un paragraphe 
avec un style de font the Arial font.</p>
</body> 
</html>
\end{lstlisting}
\includegraphics[width=0.5\linewidth]{img/metamodele/css-font.png}
\caption{Exemple CSS\label{css-font}} \end{center} \end{figure}


\paragraph{Modèle de boîte}

CSS génère pour chaque élément de l'arbre du document (DOM) une boîte
rectangulaire. Les boîtes rectangulaire sont conformes à un modèle
de boîte et sont agencées suivant un modèle de mise en forme décrit
en section \ref{sub:Mod=0000E8le-de-mise}

Chaque boîte possède ainsi une aire de contenu (\emph{e.g} une texte,
une image, \emph{etc.}) entourée en option par une aire d'espacement,
une aire de bordure et une aire de marge (\emph{e.g }figure \ref{css-modeleboite}).

\begin{figure}[htb]
\begin{center} 
\includegraphics[width=0.5\linewidth]{img/metamodele/css-modeleboite.png}
\caption{Modèle de boîte\label{css-modeleboite}} \end{center} \end{figure}


\paragraph{Modèle de mise en forme\label{sub:Mod=0000E8le-de-mise}}

Chaque boîte se voit attribuer un type qui affecte en partie son positionnement.
Les deux principaux types sont les \emph{boîtes en bloc }et \emph{les
boîte en-ligne}. Les éléments de type bloc sont des éléments dont
le rendu visuel forme un bloc (\emph{e.g }figure \ref{css-enligne}
avec l'élément de paragraphe <p>). Les éléments de type en-ligne sont
des éléments qui n'ont pas la forme de blocs de contenu (\emph{e.g
}figure \ref{css-enligne} avec l'élément <strong>). Les boîtes en-ligne
sont placées horizontalement, l'une après l'autre, en commençant en
haut du bloc conteneur. Les blocs conteneurs sont des boîtes qui encapsulent
d'autres boîtes. Les boîtes en-bloc sont placées l'un après l'autre,
verticalement, en commençant en haut du bloc conteneur. Le schéma
de positionnement décrit est appelé \emph{flux normal}.

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.1\textwidth, 
	xrightmargin=.1\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<p>Avant de faire le truc X il est 
<strong>nécessaire</strong> de faire le truc Y avant.
</p> 
\end{lstlisting}
\caption{Exemple élément en-line\label{css-enligne}} \end{center} \end{figure}

Une fois le \emph{flux normal} calculé, il est possible de le modifier. 

Un premier mécanisme possible est le positionnement relatif. La position
de la boîte est exprimée en propriété de décalage par rapport à son
bloc conteneur : 
\begin{itemize}
\item 'top' : définit le décalage du bord haut de la marge d'une boîte sous
le bord haut de la boîte du bloc conteneur. 
\item 'right' : définit le décalage du bord droit de la marge d'une boîte
à gauche du bord droit de la boîte du bloc conteneur. 
\item 'bottom' : définit le décalage du bord bas de la marge d'une boîte
au-dessus du bord bas de la boîte du bloc conteneur. 
\item 'left' : définit le décalage du bord gauche de la marge d'une boîte
à droite du bord gauche de la boîte du bloc conteneur. 
\end{itemize}
Un deuxieme mécanisme est le positionnement flottant. Une boîte flottante
est déplacée vers la gauche ou la droite sur la ligne courante du
\emph{flux normal}. Le contenu du document s'écoule alors le long
des flancs de cette dernière.

Un troisième mécanisme est le positionnement absolu. La boîte est
retirée du \emph{flux normal} et est positionnée par rapport à son
bloc conteneur. La différence avec le positionnement relatif est que
le positionnement de la boîte n'a aucun effet sur les boîtes du même
niveau de parenté. Ces boîtes peuvent, ou non, cacher les autres boîte.


\paragraph{Avant-plan et d'arrière-plan}

Les propriétés CSS permettent aux auteurs la spécification d'une couleur
d'avant-plan et d'arrière-plan pour un élément. La couleur d'arrière-plan
peut être une couleur ou une image . L'arrière-plan correspond aux
aires de contenu et, d'espacement et de bordure. Le couleur d'avant-plan
correspond à la couleur du contenu de texte d'un élément. 


\paragraph{Les polices}

CSS permet de pouvoir spécifier l'utilisation de plusieurs représentation
pour les caractères textuelles : \emph{la} \emph{police}. Une liste
exaustive de propriètés permettent de spécifier la police d'un élément
contenu dans une boîte. On peut spécifier par exemple une famille
de police (serif, sans-serif), le style de la police (italic, oblique),
la taille, \emph{ect.}


\paragraph{Les textes}

CSS définies la représentation visuelle des caractères, des caractères
blancs, des mots et des paragraphes. On peut spécifié un alinéa pour
la première ligne du texte dans un bloc ('text-indent'), l'alignement
d'un contenu en-ligne dans un élément de type bloc ('text-align'),
le comportement de l'espacement entre les caractères du texte ('letter-spacing'),
\emph{ect.}
\section{Extraction de structures}
Cette partie de l'état de l'art s'intéresse à la découverte des différentes structures logiques qui composent une page web. La difficulté imposé par HTML 4 est l'absence de sémantique pour signaler les différentes ces structures  dans une page. D'autre parts la composante syntaxique extrêmement souple du langage permet de construire une infinité de construction différentes ce qui exclus des approches basées sur une analyse syntaxique.   

\subsection{Mapping \label{sub:mapping}}
Un opération de mapping est une opération qui identifie une correspondance entre deux structures, ici des arbres. Une page HTML peut être vue comme un arbre dont les sommets sont étiquetés et ordonnés. Nous donnons ci-dessous une définition formelle d'un mapping (introduite par \citep{tai1979tree}).
~\\
\begin{defn*}
Un Mapping $M$ de l'arbre T1 vers l'arbre T2 est un ensemble de paires 
ordonnées d'entiers (i, j), $1\leqslant i\leqslant n1$, $1\leq j\leq n2$,
satisfaisant les conditions suivantes, pour tous $(i1,j1),(i2,j2)\in M$:\end{defn*}
\begin{itemize}
\item i1=i2 si et seulement si j1=j2 (one-to-one condition)
\item t1{[}i1{]} est à gauche de t1{[}i2{]}, si est seulement si t2{[}j1{]}
est à gauche de t2{[}j2{]} (préservation de l'ordre des n\oe uds frères)
\item t1{[}i1{]} est un ancêtre de t1{[}i2{]} si et seulement si t2{[}j1{]}
est un ancêtre de t2{[}j2{]} (préservation de l'ordre des ancêtres)
\end{itemize}
La première condition établie qu'une arête ne peut apparaître pas plus d'une fois dans le mapping, la seconde condition impose une préservation de l'ordre des n\oe{}uds frère et la dernière impose une relation d'héritage entre les n\oe{}uds de l'arbre. La figure \ref{mapping} illustre un mapping entre deux arbres.
\begin{figure}[htb]
\centering
\includegraphics[width=0.48\textwidth]{img/mapping.jpg}
\caption{Mapping entre deux arbres étiquetés et ordonnés \label{mapping}}
\end{figure} 
De manière intuitive, un mapping est la description d'une suite d'opérations d'édition (substitution, suppression et insertion) qui permet la transformation d'un arbre vers un autre. Les opérations d'édition se définissent ainsi :  

Notons $\Lambda$ un n\oe ud vide. Une opération d'édition est écrite $b\rightarrow c$ , où b et c sont soit un n\oe ud, soit $\Lambda$. Alors :
 
\begin{itemize}
\item $b\rightarrow c$ est une opération de substitution si $b\neq\Lambda$
et $c\neq\Lambda$
\item une opération de suppression si $b\neq\Lambda\doteq c$
\item une opération d'insertion si $b=\Lambda\neq c$
\end{itemize}

En appliquant différente restriction sur les opérations d'édition dans un mapping, plusieurs classes de mapping se peuvent se distinguer modélisant différentes applications pratiques. 
Les auteurs \citep{vieira2006fast} définissent une classe de mapping particulière, un mapping descendant restrictif. Ce derniers restreint les opérations d'édition sur les feuilles de l'arbre. Ce mapping permet d'identifier les sous-structures communes entre deux arbres. 

\begin{defn*}
Un Mapping $M$ de l'arbre T1 vers l'arbre T2 est descendant si seulement toute les paires $(i1,i2)\in M$ il y'a aussi une paire  $(parent(i1),parent(i2))\in M$
\end{defn*}

\begin{defn*}
Un Mapping descendant $M$ de l'arbre T1 vers l'arbre T2 est restrictif si seulement toute les paires $(i1,i2)\in M$ tel que $t1[i1]\ne t2[i2]$ il n'y a pas de descendant de $i1$ ou $i2$ dans $M$
\end{defn*}
Dans leur approche les auteurs s'intéressent à l'élimination de certaines structures logiques (publicités, bannières, etc.) dégradant les résultats des algorithmes des moteurs de recherche. Le postulat étant que les scripts de publication de contenu automatique génèrent, pour les structures nuisibles (bannière, publicité, \emph{etc.}) , les mêmes constructions d'une page à l'autre. 
Les auteurs s'intéressent à l'identification des structures logiques dans les pages web au moyen de construction de template matchant ces structures. La construction des templates repose sur un processus de mapping descendant restrictif à partir d'un échantillon de pages issu d'une collection de page. L'algorithme proposé par les auteurs construit des templates correspondants aux sous-structures communes identifiées par mapping descendant restrictif entre les pages web. Soit une constante $MaxPage$, le nombre de page pour construire un template (en moyenne un douzaine) et n la taille d'un arbre, la construction d'un template coûte $O(n^{2})$. L'opération de recherche d'un template dans une page coûte $O(n)$.
\subsection{Segmentation}
Dans cette partie nous introduisons plusieurs approches de segmentation. Dans le contexte des pages web, une opération de segmentation consiste à rassembler les n\oe{}uds du DOM suivant des propriétés prédéfinies. Le but étant de proposer une structure pour la page web traitée.

\subsubsection{Segmentation par pattern de page \label{subsub:seg-pattern}}
Les auteurs de \cite{kovacevic2002recognition} proposent dans leur papier une segmentation suivant un pattern présentation défini a priori. Cette approche  s'intéresse aux données cartésiennes des structures logiques dans une page web. L'idée étant que les concepteurs de page web suivent un même pattern de présentation ce qui permet de faire un regroupement des structures logiques suivant leurs localisations. 

Sur la base d'observation, les auteurs proposent un pattern de présentation de page web (\emph{cf. figure} \ref{zones-interets}). Le modèle construit est le suivant : 
\begin{itemize}
	\item une entête correspondant à l'emplacement de la bannière d'une page (\emph{H}), 
	\item un pied de page (\emph{F})
	\item une marge latérale gauche contenant des menus  (\emph{LF}) 
	\item une marge latérale droite contenant des menus (\emph{LR}) 
	\item du centre de la page encapsulant le contenu principal de la page ((\emph{C}))
\end{itemize}

\begin{figure}[htb]
\centering
\includegraphics[width=0.48\textwidth]{img/zone-interet.jpg}
\caption{Pattern de mise en forme de page web \label{zones-interets}}
\end{figure} 

La figure \ref{zones-interets} modélise le partitionnement définit par les auteurs avec \emph{W1} et \emph{W2} définissant respectivement la marge contenant les menus latérales gauches et la marge contenant les menus latérales droits. Chacune d'elle représente 30\% de la largeur de la page. \emph{H1} et \emph{H2} définissent respectivement l'entête de la page et le pied de page avec un hauteur de 200px pour \emph{H} et 150px pour \emph{F}. 

Les coordonnées des structures sont calculées au moyen d'un moteur de rendu graphique. 

En se basant sur ce modèle de représentation, des heuristiques sont déterminées afin de regrouper les n\oe{}uds du DOM de la page suivant le partitionnement de page proposé ci-dessus. %Ces heuristiques sont construites sur l'identification de n\oe{}uds de type \emph{TABLE} dont la surface est incluse dans les zones du modèle. 


À partir l'évaluation d'un corpus de 515 pages web, les résultats expérimentaux (publiés en 2002) montrent que le modèle proposé par les auteurs correspond bien à un modèle de présentation représentatif des pages web. Une note de \emph{Bon} est attribuée aux zones dont les objets HTML sont à plus de 50\% correctement étiquetés, c'est-à-dire que par exemple les éléments classifiés comme bannière correspondent bien à une bannière. Une note de \emph{Excellent} est attribuée aux zones dont les objets HTML sont à plus de 90\% correctement étiquetés. Une note de \emph{Mauvais} est attribuée lorsque les critères précédents ne sont pas satisfaits

\begin{center}
\begin{tabular}{c c c c c c}
	\hline
		& Bannière & Pied de page & Menu latéral gauche & Menu latéral droit  \\ \hline
	Mauvais & 41\% & 30\% & 21\% & 19\%\\
	Bon & 10\% & 15\% & 3\% & 2\% \\
	Excellent & 49\% & 55\% & 76\% & 23\% \\ 
	\hline	 
\end{tabular}
\end{center}

%D'après les auteurs 20\% des mauvais résultats sont dû au moteur de rendu graphique utilisé qui construit une mauvaise représentation des objets HTML. Les positions de ceux-ci ne correspondent pas à la position réelle qu'ils devraient avoir. Les 80\% restant, toujours selon les auteurs, devraient pouvoir être diminué en développant de meilleurs heuristiques.

\subsubsection{Segmentation par densitométrie textuelle \label{subsub:seg-densito}}
Les auteurs cherchent à distinguer les différentes informations qui structurent la page en se basant sur l'analyse de la répartition de la densité textuelle dans une page. L'hypothèse des auteurs est que la densité des différents blocs de textes dans le DOM est une propriété suffisante pour identifier la structure logique d'une page. 

La première étape consiste à identifier les différents segments textuelles atomiques de la page. La page est vue comme une séquence d'éléments de caractères entrelacés de tag HTML. Certain tag HTML segmentent le flux textuelle et d'autres non. Cette décision est prise (non pas d'après la sémantique des balises) d'après le rythme des séquences de caractères dans le flux. Par exemple si le flux passe d'une séquence de caractères courtes à une sequence de caractères longues cela signale un nouveau segment. Si on analyse une sequence de texte courte comme \emph{<a>Accueille</a><a>New</a><a>Contact</a>} suivit d'une sequence de texte longue comme \emph{<p>Une sequence de texte beaucoup plus longue</p>} va donner naissance à deux nouveaux segments.
Pour chaque segment (bloc) identifié par le processus ci-dessous. Les auteurs vont calculer une densité textuelle. Cette propriété correspond au ratio ci-dessous :
\[ p(b_{x}) = \frac{Nombre De Mots Dans b_{x}}{Nombre De Lignes Dans b_{x}} \]
%Les jetons correspondent à une séquence contingente de caractères n'étant pas des caractères d'espacement. 
Le nombre de ligne correspond au nombre de séquences de 80 caractères possibles dans le segment traité. 80 étant une valeur fixée par les auteurs, elle correspond à la taille d'une phrase moyenne (pour les pays anglophones).

La seconde étape permet de construire des blocs dans la page correspondant à la structuration logique de la page à partir des segments identifiés dans l'étape précédente. Cette construction consiste à fusionner les blocs contingents en comparant leurs densités respectifs. La fonction est définit ci-dessous :
\[ \Delta p(b_{x}, b_{y}) = \frac{|p(b_{x}) - p(b_{y})|}{max(p(b_{x}), p(b_{y}))} \]
Ce processus de grossissement est répété tant que la fonction définit ci-dessus ne satisfait pas un certain seuil. Cela conduit à la construction de blocs de fortes granularité correspondants à la structuration logique de la page. La figure \ref{seg-densitometrique} présente un exemple de structure découverte d'une page avec l'approche décrite.



\begin{comment}
Si la fonction delta est en dessous d'un certain seuil $Smax$ les blocs adjacents sont fusionnés. La fusion de segment consiste ici à joindre les lignes des blocs \emph{x} et \emph{y} en un nouveau bloc \emph{z}. La fonction boucle tant qu'elle trouve des blocs adjacents qui ne respectent pas un seuil définit à priori.
\end{comment}

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{img/densitometric-segmantation.png}
\caption{Segmentation densitométrique \cite{kohlschutter2008densitometric} \label{seg-densitometrique}}
\end{figure}

\subsubsection{Segmentation par indice visuel \label{sub:vips}}
L'approche proposée par les auteurs \cite{cai2003extracting} présente
un algorithme de partitionnement basé sur les éléments de mise en
forme des pages web. Le partitionnement extrait une structure qui regroupe
les éléments d'une page d'après leur mise en forme (\emph{e.g.} figure
\ref{vips_partitionnement}). Le postulat est que les éléments d'une
page possédant des caractéristiques de mise en forme proches, tels
que la police, la couleur, la taille, peuvent être regroupés.

\begin{figure}[H]
\centering
\includegraphics[width=300px]{img/VIPS-partionnement.jpg}

\caption{Exemple de partitionnement, (a) page (b) DOM de la page \cite{cai2003extracting}\label{vips_partitionnement}}
\end{figure}


L'algorithme exploite le \gls{DOM}%
\footnote{Document Object Model%
} de la page web. Le processus de segmentation, figure \ref{vips-process}, se décompose
en trois phases : un processus d'extraction de blocs, un processus
de détection de séparateur et un processus de reconstruction. 

Le processus d'extraction détecte les éléments du niveau courant du
DOM susceptibles de former un contenu cohérent. Cette détection repose
sur des séparateurs explicites : on sait que certains éléments délimitent
le contour d'un contenu (par exemple les balises <DIV>). Mais elle repose également
sur une fonction de distance visuelle comparant les n\oe uds parents
et frères du n\oe ud courant : une balise <DIV> a de grandes chances de
délimiter un contenu sémantiquement différent du n\oe ud parent si la
couleur de fond est différente de celle de ce dernier. Pour chaque
n\oe ud, l'algorithme vérifie s'il forme un bloc ou non. Si oui, il
associe un degré de cohérence au bloc. Ce degré de cohérence est un
indicateur de l'importance sémantique du bloc. Si non, il est appliqué
le même processus aux enfants du n\oe ud. Quand tous les n\oe uds du bloc
courant sont extraits, ils sont mis dans un pool. 

Des séparateurs entre les blocs sont ensuite détectés. L'algorithme
détecte ici des séparateurs implicites, c'est-à-dire n'apparaissant
pas dans la structure HTML. L'opération du calcule de séparateurs à une complexité de $O(n^{2})$. Les séparateurs implicites sont les espaces
entre les blocs d'un pool. Un poids est attribué à chaque séparateur
suivant son importance (par exemple, plus l'espacement entre deux
blocs est grand, plus le poids sera élevé). Ce poids est un indicateur
de différence sémantique entre les blocs adjacents. Plus le poids du
séparateur est élevé entre deux blocs, plus leur contenu sera sémantiquement
éloigné.

Une construction hiérarchique des blocs est créée. Cette construction
hiérarchique repose sur le degré de cohérence attribué à chaque bloc. 

Pour chaque nouveau bloc de la structure hiérarchique construite,
l'algorithme teste le degré de cohérence attribué par rapport à un
seuil de cohérence défini. Ce seuil est défini suivant la granularité
de la structure que l'on veut en sortie de l'algorithme. Si le degré
de cohérence n'est pas supérieur au seuil de cohérence, le bloc est
de nouveau proportionné. La structure finale est construite après que
tous les blocs soient traités. 

\begin{figure}[htb]
\centering
\includegraphics[width=300px]{img/VIPS-processus.jpg}

\caption{Algorithme de segmentation \cite{cai2003extracting} \label{vips-process}}
\end{figure}

\subsection{Synthèse \label{extraction-synthese}}

%\begin{tabular}{ | l | l | l | l | l |}
%	\hline
 %  		   & Globale & Locale & \\ \hline \hline
  % Mapping &  &  &  & \\ \hline
   %Segmentation par zone & \checkmark &  & \checkmark & \\ \hline
   %Segmentation par densitométrie &  &  &  & \\ \hline
   %Segmentation par indice visuel &  &  &  & \\ \hline
%\end{tabular}
 %\faire une synthèse sur les différents modes de reprénsentation des objets HTML (définition des segments)
 L'approche par correspondance de structure (section \ref{sub:mapping}) entre les pages permet d'isoler seulement des fragments de page, la structure globale de celle-ci n'apparaît pas. Les approches par segmentation sont plus intéressante car elle propose un découpage reflétant une structure globale. 
 La segmentation par pattern (section \ref{subsub:seg-pattern}) est une approche efficace dans la plupart des cas. Cependant toutes les pages web ne suivent pas le même modèle de présentation. La segmentation par densitométrie (section \ref{subsub:seg-densito}) est une méthode indépendante du pattern de présentation de la page. Cependant, le problème avec cette approche est qu'elle ne fournit pas le moyen de prendre en compte les possibles écarts entre la structure décrite par HTML et le rendu finale. c'est-à-dire que des n\oe{}uds du DOM peuvent être proches du la structure du DOM puis avec l'application de propriétés de mise en forme, très éloignés avec le rendu visuel en particulier avec les propriétés de positionnements absolues décrites en section \ref{sub:Mod=0000E8le-de-mise}. La segmentation par indices visuels (section \ref{sub:vips}) comble les faiblesses des deux méthodes précédentes. L'inconvénient de cette approche se situe au niveau de l'algorithme de calcule des séparateurs qui est une opération coûteuse $O(n{2})$.
 \fp{Expliquer tous les inconvénients des séparate }


 
\section{Annotation de structures}
\subsection{Approche fonctionnelle} 
\citep{chen2001function} propose une approche afin de découvrir la fonctionnalité des éléments composants une page web. Les différents éléments sont des objets qui apportent une information ou réalisent une action. Ces objets peuvent soit être des objets basiques ou soit des objets composites. Les objets basiques et composites sont le résultat d'un prétraitement (non décrit dans le papier) qui retourne une structure arborescente dont les feuilles forment des objets basiques et les n\oe{}uds ayant des descendants forment des objets composites. Afin de qualifier chaque n\oe{}uds les auteurs proposent un ensemble de propriétés pour attribuer une classe à chaque objet (basique et composite).
L'hypothèse est que chaque objet au travers de ces fonctionnalités reflète l'intention de son auteur, c'est-à-dire ça sémantique.

La structure des objets basiques est décrit d'après les propriétés fonctionnelles ci-dessous : 
\begin{description}
	\item[Présentation]: l'agencement (\emph{e.g} alignement gauche), le type de média (\emph{e.g} texte, image). 
	\item[Sémantème]: la sémantique associée par le HTML. Par exemple la balise \emph{h1} définit un titre
	\item[Décoration]: une valeur entre 0 et 1 qui définit dans quelle mesure l'objet sert à l'amélioration de l'esthétisme de la page (lignes, séparateurs, et les objets avec des couleurs d'arrière plan). 
	\item[Hyperlien]: l'objet possède t il des hyperliens? et où pointent-ils vers (domaine)? 
	\item[Interaction]: Les types d'interactions de l'objet. Affichage, soumission d'informations, sélections etc.
\end{description}
Les propriétés listées précédemment sont calculées d'après l'analyse du code HTML. Par exemple l'attribut \emph{href} (spécifie la destination d'un lien) est utilisé pour calculer la propriété d'hyperlien. 

 
Les objets composites sont donc une composition d'objets basiques et/ou composites. Ils sont caractérisés par les propriétés décrites plus haut et les relations des objets qui le composent. Ces relations sont classées ainsi :
\begin{description}
	\item[Relation de Classification] : 
		\begin{itemize}
			\item Complémentaire : les enfants de la racine de l'objet composite sont complémentaires à la réalisation de la fonction. Ils ne possèdent pas les mêmes propriétés basiques
			\item Parallèle : les enfants de la racine de l'objet composite ont une importance égale à la réalisation du but. Ils possèdent les mêmes propriétés basiques
			\item Présentation : verticale ou horizontale  (\emph{e.g} une liste à puce peut-etre présenté horizontalement ou verticalement)
	\end{itemize}
\end{description}

Une fois les propriétés ci-dessus établies, les auteurs définissent un ensemble de classes pour les objets :
\begin{description}
	\item [Informatif] : objets qui présentent le contenu informatif 
	\item [Navigation] : objets qui déclenche une navigation
	\item [Interaction] : objets qui fournissent des interactions avec l'utilisateur 
	\item [Décoration] : objets qui remplissent un rôle esthétique (puce de liste, séparateur, \emph{etc.})
\end{description}
La classe des objets basiques sont déterminées simplement par la valeur de ces propriétés. Un objet basique informatif possède un contenu textuel important, une image (taille significatif par rapport à l'objet). La classe des objets composites intègre les propriétés de relation. Par exemple, on peut définir un menu (appartenant à la classe des objets de navigation.) comme suit : un objets composites dont les enfants de la racine sont des objet de navigation parallèle (même propriété), le nombre d'enfant navigable est supérieur à un certain seuil \emph{Hmin} et la propriété de texte est inférieur à un seuil \emph{Lmax}

\subsection{Inférence du contenu informatif}
Une page web possède différents contenus que l'on peut classifier comme contenu informatif et contenu non informatif. Un contenu informatif est un contenu qui apporte une information dans dans la page. %On peut dire qu'un contenu est informatif quand il caractérise une page. 
 
\subsubsection{Inférence basée sur la localisation}
La vision des auteurs est que les blocs informatifs sont identifiables par leur localisation, les blocs informatifs se concentrent sur le centre de l'écran.
Les auteurs \citep{wininformative} propose un algorithme pour l'extraction du contenu informatif dans une page web.  Le contenu informatif est identifié comme étant les blocs contenant le plus de contenu textuels et se situant le plus proche du centre de l'écran.
\[ f(b_{x}) = \frac{TailleDuBlocb_{x}}{DistanceEntreLeCentreDeb_{x} EtLeCentreDelEcran} \]
\begin{comment}
 L'algorithme se décompose en trois étapes :
\begin{enumerate}
	\item Extraction des blocs sémantiques dans la page. Cette partie repose sur une méthode de segmentation similaire à l'algorithme présenté en section \ref{sub:vips}.
	\item Sélection des candidats. L'algorithme prend en entrée les blocs résultants de l'étape précédente. Pour chaque bloc est appliqué une fonction qui vérifie si il peut être candidat ou non à être un bloc informatif. Cette fonction s'intéresse aux propriétés textuelles, nombre d'hyperliens. Les détails du paramétrage de cette fonction ne sont pas précisés dans l'article.  
	\item Élection des blocs informatifs. L'algorithme prend en entrée la sortie de l'étape précédente.
	Dans un premier temps, pour chaque bloc est calculé une valeur signalant l'importance de son contenu informatif.
\end{enumerate}
\end{comment}
\subsubsection{Inférence basée sur la fréquence de répétition}
\begin{comment}
La vision des auteurs est que les contenus informatifs sont des parties distingues (singulière) dans une page alors que les contenus non informatifs sont des partis redondantes. 
Calcule une valeur d'entropie pour chaque bloc. Cette valeur d'entropie permet de partionner l'ensemble des blocs soit en bloc informatif, soit en bloc non-informatif suivant un seuil déterminé. 
Le calcule de la valeur d'entropie est réalisé suivant le distribution des mot clés dans un cluster de page (ensemble de page d'un site web)
Une première phase est le découpage de la page en bloc, ce découpage se base sur les balises table (l'approche ne traite que les site organisées avec les balises tabulaires)
\end{comment}
\fp{Intégration d'un article sur l'identification du contenu informatif dans une page. Propose le calcule d'une valeur d'entropie basée sur une mesure nommée TF-IDF permettant le partitionnement des structures logiques en contenu informatif ou non informatif}

\subsection{Synthèse}
\fp{intégration d'une synthèse}


\chapter{Réalisation \label{chap:realisation}}
\section{Méta-modèle \label{sec:meta-modele}}

HTML 4 fournit une sémantique pour définir des n\oe{}uds de contenu (image, texte) mais pas pour des structures logiques plus élaborées (article, menu, bannière etc.). Par exemple, la figure \ref{menu_html4} présente deux structures logiques extraites d'une des pages du site web \textit{lemonde.fr} et d'une page des pages du site web \textit{www.eclipse.org}. D'après cette figure, on sait que les éléments textuels sont des liens de navigation signalés par la balise \textit{<a href=...>}, mais il n'y a pas d'informations sémantiques sur la structure qu'ils composent (un menu de navigation). Ce premier point pause un problème pour l'expression de préférence sur de telles constructions. 

La contribution de ce méta-modèle vient apporter une composante (dimension) sémantique aux langages de publication du web.
Cette sémantique est la concrétisation des différents concepts définies par les auteurs de pages web et ce qu'en comprend le lecteur. Dans un premier temps cette composante est essentielle pour l'expression des souhaits de personnalisation de l'utilisateur. En second lieu, le méta-modèle nous fournit une couche d'abstraction permettant de s'affranchir de la diversité de représentation des données. 

 
Les parties suivantes proposent notre modélisation de HTML 4 et 5 puis introduisent notre méta-modèle. %Puis notre méta-modèle Le méta-modèle se décompose en deux parties. Une partie décrivant la sémantique des principales structures logiques (section \ref{sub:Mod=0000E8le-de-mise}) utilisées dans la conception des pages. Une seconde partie pour la description de la mise en forme associée à chaque élément du méta-modèle précédent.


\subsection{Méta-modèle HTML 4 et  5 \label{subsect-metamodelhtml}}
\subsubsection{HTML 4}
\paragraph{Méta-modèle HTML 4}
HTML 4 discrimine ces éléments de langage suivant que pendant l'affichage du flux de contenu les éléments produisent un retour à la ligne ou non (\emph{cf.} section \ref{sub:Mod=0000E8le-de-mise}). Le modèle de HTML 4 repose plutôt sur une description des aspects de mise en forme des éléments que sur la sémantique des structures construites.
Nous proposons dans la figure \ref{metamodele_HTML4} un méta-modèle de HTML 4. Les principales méta-classes sont :
\begin{itemize}
	\item La méta-classe \emph{Bloc} dont les instances provoquent un retour à la ligne. Par exemple, une division (<div>) ou un paragraphe (<p>) génère un retour à la ligne
	\item La méta-classe \emph{Inline} dont les instances ne provoquent pas de retour à la ligne. Par exemple, \emph{<p>Bonjour tous le <strong>monde</strong></p>}, la balise \emph{<strong>} ne provoque pas de retour à la ligne dans le paragraphe.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\linewidth]{img/metamodele/HTML4.png}
\caption{Méta-modèle HTML 4\label{metamodele_HTML4}}
\end{figure}

\paragraph{Instance}
HTML 4 délimitent les structures logiques de plus haut niveau à l'aide d'éléments structurels génériques (\emph{div}) et leurs propriétés de mise en forme (bloc/inline). Si l'on instancie le premier menu de la figure \ref{menu_html4}, on voit bien que HTML 4 ne possède pas un niveau d'expressivité suffisant pour exprimer une préférence, par exemple, sur un menu de navigation. 

{\fp{faire un schéma}

\subsubsection{HTML 5}
\paragraph{Méta-modèle HTML 5}
HTML 5 propose une classification plus élaborée qui ne se concentre plus sur les aspects de mise en forme mais sur la sémantique des différentes structures. Nous proposons dans la figure \ref{metamodele_HTML5} un méta-modèle de HTML 5. Ci-dessous les principales classes de balises : 

\begin{itemize}
\item Les instances de la méta-classe \textbf{Sectioning} définissent le contenu comme des éléments qui créent une nouvelle section dans le plan d'un document. Les instances de <section>, <article>, <nav>, <aside> héritent de cette dernière. 

%\item La méta-classe \textbf{Header} définit l'entête d'un document. Pour une page web cela correspond à la bannière.
%\item La méta-classe \textbf{Footer} définit le contenu comme étant des éléments de pied de page dans un document.

\item Les instances de la méta-classe \textbf{Phrasing} définissent les éléments de langage qui peuvent définir un texte. Par exemple, la méta-classe \emph{Strong} définit un texte important. 

\item Les instances de la méta-classe \textbf{Embedded} définissent un contenu importé d'un autre espace de nom. C'est le cas par exemple des instances de la méta-classe \textit{Video}, son contenu est importé de l'extérieur.

\item La instances de la méta-classe \textbf{Interactive} définissent un contenu conçu pour un interaction avec l'utilisateur. Par exemple, pour une instance de la méta-classe \textit{Video}, l'utilisateur va pouvoir arrêter ou mettre en pause la vidéo. 
\end{itemize}


\begin{landscape}
\begin{figure}[H]
\centering
\includegraphics[scale=0.6]{img/metamodele/HTML5.png}

\caption{Méta-modèle HTML 5\label{metamodele_HTML5}}
\end{figure}
\end{landscape}

\paragraph{Instance}
Si l'on compare le méta-modèle de HTML 4 avec celui de HTML 5, on constate dans un premier temps que la mise en forme ne fait plus partie du méta-modèle. Le concept d'éléments en bloc et en-ligne est délégué à CSS. Dans un second temps il intègre des éléments de langage spécifique (et non plus générique) pour la description des structures construites. Si l'on instancie le premier menu de la figure \ref{menu_html4}, avec des éléments de HTML 5, il est possible d'exprimer, par exemple, une préférence sur l'accessibilité des menus de navigation dans une page. 

Une préférences sur l'accessibilité serait, par exemple, que la taille du texte du menu de navigation soit supérieur à 15 pixels : 
\begin{lstlisting}[frame={single}, xleftmargin=.1\textwidth, xrightmargin=.1\textwidth]
navigation.style.size < 15px
\end{lstlisting}

{\fp{faire un schéma}


\subsection{Méta-modèle conçu pour le projet}
\subsubsection{Modèle de contenu \label{subsec-modelcontenu}}
La conception de cette partie du méta-modèle repose sur les éléments conceptuels identifiés dans la sous-section \ref{subsect-metamodelhtml} en étendant l'expressivité de notre méta-modèle aux \emph{Landmarks} de ARIA (Bannière, Contenu principal, \emph{etc.}) et aux éléments de langage décrivant les éléments d'interaction. 

%\begin{comment}langage pas adapté à la description d'interface graphique mais plutot de document\end{comment}
Chaque élément du méta-modèle (\emph{cf.} figure \ref{modele-metamodele} et \ref{widget-metamodele}) construit va nous permettre de qualifier les éléments d'une page. Une première partie décrie les éléments de structuration. 

Les principales méta-classes de structures sont :
\begin{itemize}
	\item Les instances de \textit{SECTION} peuvent contenir des instances de structure.  
	
	\item Les instances de \textit{REGION} correspondent à un regroupement par thématique des éléments qu'elles encapsulent, c'est-à-dire des éléments que l'on peut regrouper sur la base d'une information commune.

	\item Les instances de \textit{LANDMARK} spécifie \textit{REGION} en proposant des thématiques récurrentes dans la conception des pages web actuelles. On a par exemple la méta-classe \textit{NAVIGATION} pour les menus ou la méta-classe \textit{BANNER} pour la bannière d'une page web.
		
	\item Les instances de \textit{SECTIONHEAD} synthétisent le contenu d'instances de \textit{SECTION}. Il possède une portée locale, ceux-ci synthétises le contenu de l'instance de type \textit{SECTION} le plus proche. 

	\item Les instances de \textit{Texte} définissent un contenu textuel (\emph{e.g} une emphase, une citation, \emph{etc.}). Elles ne peuvent contenir que des éléments textuels ou des instances de la méta-classe \textit{Texte}.
\end{itemize}

Un seconde partie de ce méta-modèle décrit les éléments d'interaction dans une page web. Les principales méta-classes sont :
\begin{itemize}

	\item Les instances de \textit{INPUT} qui permettent des entrées utilisateurs. Elles correspondent aux balises de type \textit{<input>} de HTML4. 

	\item Les instances de \textit{COMMAND} qui réalisent des actions mais ne reçoivent pas d'information en entrée. Par exemple les balises de type \textit{<a href=...>} déclenche une action de navigation ou une balise de type \textit{<input type=button>} déclenche l'envoie de données d'un formulaire vers un serveur. 

	\item Les instances de \textit{SELECT} qui permettent de faire des sélections parmi un ensemble de choix.
\end{itemize}


\begin{landscape}
\begin{figure}[htb]
\begin{center} 
\includegraphics[width=.7\linewidth]{img/metamodele/modele-contenu-widget.png}
\caption{Méta-modèle interaction\label{modele-metamodele}} \end{center} \end{figure}
\end{landscape}

\begin{landscape}
\begin{figure}[htb]
\begin{center} 
\includegraphics[width=.9\linewidth]{img/metamodele/modele-contenu-structure.png}
\caption{Méta-modèle contenu\label{widget-metamodele}} \end{center} \end{figure}
 \end{landscape}
\subsubsection{Modèle de mise en forme}
Le méta-modèle de mise en forme (\emph{cf.} figure \ref{css-metamodele}) repose sur les éléments du langage CSS. Cette partie est un support essentiel à l'expression des préférences, en particulier pour les préférences liées à l'accessibilité. Les principales méta-classe sont :

\begin{figure}[H]
\begin{center} 
\includegraphics[width=\linewidth]{img/metamodele/CSS.png}
\caption{Méta-modèle\label{css-metamodele}} \end{center} \end{figure}

\begin{itemize}
\item Les méta-classes \textit{BOX} qui représentent le concept de bloc conteneur. Elles vont permettre d'exprimer des préférences sur l'accessibilité tels que l'espacement plus ou moins fort (propriété padding) avec les éléments connexes à ces dernières. Mais encore sur les contrastes avec la propriété de couleur d'arrière plan et la propriété de couleur de la méta-classe \textit{Text}.


\item Les méta-classes \textit{TEXT}  décrivent la représentation visuelle des
caractères, mots et paragraphes contenus dans une boîte. Par exemple, la propriété \emph{letter-spacing }spécifie l'espace entre les lettres.

\item Les méta-classes \textit{POLICE} décrivent la représentation visuelle des caractères. Dans le contexte de l'amélioration de l'accessibilité il peut être intéressant, par exemple, d'éviter certain type de \emph{Font} qui peuvent rendre un contenu plus illisible pour certaine pathologie.
\end{itemize}

\subsubsection{Instance du méta-modèle}
\subsection{Discussion}
La réalisation de ce méta-modèle répond aux premiers besoins identifiés pour l'expression de souhait de personnalisation de page web pour en améliorer l'accessibilité.  
On peut tout à fait exprimer des contraintes sur le contraste d'un texte 
\begin{lstlisting}[frame={single}, xleftmargin=.1\textwidth, xrightmargin=.1\textwidth]
context Paragraph	inv :
	if self.style.color = red then
		self.style.background-color != green
\end{lstlisting}
Exprimer des préférences sur des structures plus haut niveau
\begin{lstlisting}[frame={single}, xleftmargin=.1\textwidth, xrightmargin=.1\textwidth]
	Banner.style.display = hidden 
\end{lstlisting}

Le méta-modèle ne traite que des aspects de présentation dans une page web. Des perspectives seraient la réification des aspects de navigation (fils de lecture dans le contenu) et les aspects dynamiques (interactions des éléments de la page avec l'utilisateur). 

\section{Extraction de structures \label{sec:extraction}}
\subsection{Introduction}
Dans cette partie nous traitons de l'extraction des structures logiques dans une page. Nous nous intéresserons à leur signification dans la section \ref{sec:annotation}. Le DOM d'une page web est constitué d'un ensemble de structures logiques, des regroupements d'éléments qui remplissent une fonction dans la page. 

La première difficulté est que ces structures logiques ne sont pas annotées de manières explicites dans le DOM de la page. Nous voulons dire par explicite qu'il n'y pas d'annotations standards pour identifier et qualifier une structure. L'annotation se fait de manière implicite par l'attribution de classes d'appartenance (\emph{cf.} \ref{html4-classe})permettant au navigateur web d'associer des mises en formes aux n\oe{}uds du DOM. La sémantique des classes n'est alors connue que par la concepteur. 

Une seconde difficulté provient de l'hétérogénéité du contenu. Une page définit plusieurs contenus (bannière, pied de page, article, \emph{etc.}). Si l'on regarde le listing \ref{lst:heterogene}, dans le contexte de notre méta-modèle, on doit identifier trois structures logiques différentes : une bannière, un document, un menu. Les éléments de langages de HTML 4 ne permettent pas de signaler les différents contenu dans une page.

\lstset{language=HTML}
\begin{lstlisting}[frame=single, caption={Exemple contenu homogène}, label={lst:homogene}]
<div id='document'>
  <div>
	<h1>Introduction</h1>
	<p>une longue introduction (...)</p>
  </div>
  <div>
	<h1>Section 1</h1>
	<p>un long paragraphe (...) </p>
	<p>un second long paragraphe (...)</p>
  </div>
</div>
\end{lstlisting}

\begin{lstlisting}[frame=single, caption={Exemple contenu hétérogène}, label={lst:heterogene}]
<div id=page>
 <div id='banner'></div>
 <div id='menu-principal'></div>
 <div id='document'>
   <div>
	 <h1>Introduction</h1>
	 <p>une longue introduction (...)</p>
   </div>
   <div>
	 <h1>Section 1</h1>
	 <p>un long paragraphe (...)</p>
	 <p>un second long paragraphe (...)</p>
   </div>
</div>
<div id='footer'></div>
</div>
\end{lstlisting}

Une troisième difficulté est la diversité des structures. HTML 4 possède une composante syntaxique extrêmement souple. On veut exprimer par composante syntaxique extrêmement souple le faite que HTML 4 autorise la construction d'un ensemble virtuellement infini de structures logiques. Si l'on reprend la figure \ref{menu_html4}, on voit que l'on a deux structures logiques différentes ayant la même sémantique (un menu). Cette diversité pose des difficultés pour l'extraction de structure. 


\begin{figure}[htb]

\lstset{ 
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.1\textwidth, xrightmargin=.1\textwidth
}
\begin{lstlisting}[frame=single, language=html]
<div id="header-nav">
<ul>
	<li><a href='...'>Home</a></li> 
	<li><a href='...'>Downloads</a></li> 
	<li><a href='...'>Users</a></li> 
	<li><a href='...'>Members</a></li> 
	<li><a href='...'>Committers</a></li> 
	<li><a href='...'>Resources</a></li> 
	<li><a href='...'>Projects</a></li> 
	<li><a href='...'>About Us</a></li> 
</ul>
</div>
\end{lstlisting}

\lstset{ 
  basicstyle=\footnotesize, frame=tb,
  xleftmargin=.1\textwidth, xrightmargin=.1\textwidth
}
\begin{lstlisting}[frame=single, language=html]
<div>
<p>
	<a href='...'>Le Monde</a><a>Télérama</a>
	<a href='...'>Le Monde diplomatique</a>
	<a href='...'>Le Huffington Post</a>
	<a href='...'>Courrier international</a>
	<a href='...'>La Vie</a>
	<a href='...'>au Jardin</a>
</p>
</div>
\end{lstlisting}
\caption{Exemple de différentes conceptions de menu avec HTML4\label{menu_html4}}
\end{figure}

Une quatrième difficulté est le possible écart dans la structure du DOM et le rendu final. Les n\oe{}uds dans le DOM peuvent être proche mais au moment de la mise en page par le navigateur peuvent être très éloignés. Ceci est dû aux possibilités offertes par CSS de modifier l'algorithme du moteur graphique des navigateurs (\emph{c.f} \ref{sub:Mod=0000E8le-de-mise}).

Pour finir les concepteurs de page web ne tiennent pas toujours compte des spécifications des langages dans la création des pages. Donnons deux exemples. Dans le listing \ref{lst:monde} on constate que l'étiquette \emph{P} n'est pas utilisée pour structurer un textes en paragraphe mais pour structurer des liens de navigation en menu. Dans le listing \ref{lst:google-style} on voit que les étiquettes \emph{DIV} ne sont utilisées pour indiquer une division mais pour faire de la mise en forme (en appliquant un style). 

  
\begin{lstlisting}[frame=single, language=html, caption={Extrait menu lemonde.fr}, label={lst:monde}]
<p>
	<a href='...'>Le Monde</a>
	<a href='...'>Télérama</a>
	<a href='...'>Le Monde diplomatique</a>
	<a href='...'>Le Huffington Post</a>
	<a href='...'>Courrier international</a>
	<a href='...'>La Vie</a>
	<a href='...'>au Jardin</a>
</p>
\end{lstlisting}

\lstset{language=HTML}
\begin{lstlisting}[frame=single, caption={Pattern page d'accueil google.fr}, label={lst:google-style} ] 
<div class='style1'>
 <div class='style2'>
  <div class='style3'>
    	<form>...</form>  
  </div>
 </div>
</div>
\end{lstlisting}

Toute ces contraintes posent le cadre de l'élaboration de notre méthode d'extraction des structures logiques dans une page. 
Plusieurs méthodes sont décrites dans l'état de l'art qui présente une synthèse des différentes approches (\emph{cf.} \ref{extraction-synthese}). Nous choisissons une approche qui s'intéresse aux propriétés de mise en forme pour proposer un regroupement des n\oe{}uds du DOM reflétant des structures logiques. Ainsi la conception des différentes heuristiques intègrent les propriétés de mise en forme associées aux neuds du DOM. Dans un premier temps nous proposons ainsi plusieurs concepts pour l'élaboration d'heuristiques. Ces concepts définissent une modèle de page web intégrant les aspects de mise en forme et de structuration des éléments  

\subsection{Méthode}
\subsubsection{Description modèle de page}
La méthode choisit est un processus itérative basée sur des découpages successives de la structures de la page. Depuis la racine de la structure d'une page, on va parcourir (suivant un parcours préfixé) ses n\oe{}uds. Pour chaque n\oe{}ud parcouru on va soit l'annoter comme une structure logique, soit le diviser. Ces décisions sont prises sur la base d'heuristiques définies a priori. Dans le premier cas on rappliquer le processus de découpage, dans le second cas on continue le parcours. Les itérations sont stoppées lorsque tous les n\oe{}uds atomiques sont identifiés. Le résultat est la construction d'un arbre reflétant la composition des structures dans la page. %Cette structures nous permet par la suite d'appliquer des algorithmes permettant d'inférer une sémantique aux n\oe{}uds de l'arbre.

Nous définissons ci-dessous les concepts utiles à l'élaboration des heuristiques.   
Nous proposons une classification des n\oe{}uds (étendue de \citep{cai2003extracting}, une fonction de distance visuelle et de calcule du poids d'une n\oe{}euds : 
\begin{description}
	\item[N\oe{}uds conteneur] : l'ensemble des n\oe{}uds qui peuvent contenir d'autres n\oe{}uds (conteneurs et données). Ils ont un rôle de structuration logique. Par exemple, \emph{<p>} peut structurer un ensemble de n\oe{}uds de données textuelles pour former un paragraphe. Ils correspondent aux n\oe{}uds avec la propriété display de CSS (display : 'bloc')
	 
	\item[n\oe{}uds de mise en forme] : l'ensemble des n\oe{}uds qui n'ont pas un rôle de structuration mais viennent signaler une mise en forme aux n\oe{}uds de données. Par exemple les balises \emph{<strong>} signalent de mettre en gras un texte mais ne structure pas les n\oe{}uds qu'elles encapsulent. Si les n\oe{}uds conteneurs décrivent une structure logique, les n\oe{}uds de mise en forme décrivent une structure physique. Ils correspondent aux n\oe{}uds avec la propriété display de CSS (display : 'inline')
	
	\item[N\oe{}uds de contenus] : l'ensemble des n\oe{}uds qui contiennent un contenu atomique. Les contenus atomiques correspondent aux éléments multimédias dans la page :  texte, image, video, etc. On inclus les balises <img>, <object> et <h1-h6>
	
	\item[N\oe{}uds de contenus virtuelles] : l'ensemble des n\oe{}uds qui contiennent des n\oe{}uds de contenu encapsulés par des n\oe{}uds de mise en forme. Par exemple, \emph{<P>Ceci est un paragraphe avec un <strong>n\oe{}euds de mise en forme</strong></P>}. \emph{P} est un n\oe{}ud virtuel
	
	\item[N\oe{}uds de descriptions] : n\oe{}uds qui donnent des méta-informations sur une page. Par exemple, la balise \emph{<style>} renseignent sur la feuille de style à appliquer à la page. Ce sont des balises utilisé pour renseigner le navigateur sur la page
	
	\item[N\oe{}uds visibles] : l'ensemble des n\oe{}uds qui sont visibles au travers du navigateur (exclus les noeuds de description) 
	
	\item[Fonction de distance visuelle ] : cette fonction compare les propriétés de mise en forme associées à chaque n\oe{}uds pour déterminer si ils sont visuellement distant ou non. Par exemple, la fonction renvoie vraie si le n\oe{}uds ne partagent pas les mêmes propriétés :
	\begin{itemize}
		\item d'arrière-plan
		\item de bordure
		%\item avant-plan, police, 
		\item etc.
	\end{itemize}	 
	\item[Taille du n\oe{}uds] : pour chaque n\oe{}ud, nous calculons taille. Le calcule de la taille prend en compte les éléments contenu par le n\oe{}ud et sa position dans le DOM. 
	
	$ Taille(b_{x}) = \frac{TailleDunoeud_{x}}{100} * TailleDunoeudRacine $
\end{description}
\subsubsection{Heuristiques}
L'élaboration des différentes heuristiques ci-dessous repose sur les concepts décrient précédemment.

\begin{description}
	\item[règle 1] : Si le n\oe{}ud n'est pas un n\oe{}uds de contenu et qu'il ne possède pas de n\oe{}uds valident alors on ne traite pas ce n\oe{}ud
	\item[règle 2] : Si le n\oe{}ud possède un seul enfant valide et cet enfant n'est pas un n\oe{}uds de données alors on parcours ce n\oe{}ud
	\item[règle 3] : Si les enfants du n\oe{}ud sont des n\oe{}uds de données ou des n\oe{}uds de données virtuelles alors on annote ce n\oe{}ud comme formant une structure logique
	\item[règle 4] : Si l'un des enfants du n\oe{}ud est un n\oe{}ud conteneur et que sa taille est inférieure à un certain seuil alors on parcours alors on parcours ce n\oe{}ud
	\item[règle 5] : Si l'un des enfants du n\oe{}ud est étiqueté par \emph{HR} alors on extrait les premiers n\oe{}uds valides successeurs et prédécesseurs comme des structures logiques de chaque enfant étiqueté par \emph{HR}
	\item[règle 6] : Si la somme des enfants visibles de la surface du n\oe{}ud est supérieur à ce dernier, alors on parcours le n\oe{}ud	
	\item[règle 7] : Si le premier enfant valide du n\oe{}ud est un n\oe{}ud de titre alors on alors on extrait ce n\oe{}ud comme une structure logique
	\item[règle 8] : Si la fonction de distance visuelle est vérifiée (renvoie vraie) pour au moins un enfant du n\oe{}ud alors on parcours ce n\oe{}ud et on extrait comme structure logique le n\oe{}ud qui a vérifié la fonction de distance
	\item[règle 9] : Si le plus grand enfant valide est plus petit qu'un seuil prédéfini alors on extrait ce n\oe{}ud comme une structure logique
	\item[règle 10] : par défaut nous parcourons le n\oe{}ud
\end{description}

%\paragraph{Règle 1} Cette règle élimine les n\oe{}uds qui 

\paragraph{Règle 2} La règle 2 permet de découvrir des structures logiques plus intéressantes dans une page. D'après l'exemple \ref{lst:google}, cette règle va permettre d'atteindre le n\oe{}ud \emph{center} contenant la bannière de la page et la barre de recherche.
\lstset{language=HTML}
\begin{lstlisting}[frame=single, caption={Pattern page d'accueil google.fr}, label={lst:google} ] 
<span id='body'>
 <center>
    <div id='banner'>...</div>
    <div id='search-bar'>...</div>
 </center>
</span>
\end{lstlisting}


\paragraph{Règle 3} La règle 3 nous permet de récupérer les n\oe{}uds de données. Ce sont les n\oe{}uds que l'on considère comme atomique.
\lstset{language=HTML}
\begin{lstlisting}[frame=single, caption={Extrait de www.eclipse.org/actf/}, label={lst:eclipse-style} ] 
<p>ACTF 1.1 including Visualization SDK is now available. 
Please visit <a href="downloads/index.php">downloads page</a> 
and get ACTF components!</p>
\end{lstlisting}

\paragraph{Règle 4} Si l'on reprend \ref{lst:google} après l'application de la règle 2 on va découvrir des structures logiques plus intéressantes qui sont le bannière de la page d' accueil de GOOGLE et la bar de recherche.

%\paragraph{Règle 5} La balise HR est employé pour séparer des contenu
%\begin{lstlisting}[language=HTML, frame=single, caption={}, label={lst:}]
%\end{lstlisting}
\paragraph{Règle 6} Dans \ref{lst:wikipedia} le n\oe{}ud \emph{mw-navigation} est un n\oe{}ud qui regroupe deux menus de navigation pour leurs appliquer une style. Cette proximité des deux n\oe{}uds ne reflète pas le rendu final puisque après le mise en page les deux n\oe{}uds sont très éloignés (l'un dans l'entête de page, l'autre dans la marge gauche). On peut connaître cette information en regardant la surface des n\oe{}uds attribuées par le moteur graphique, en effet la surface du n\oe{}ud identifiée par \emph{mw-navigation} est inférieur à la somme de la surface des n\oe{}uds qu'il contient.
\lstset{language=HTML}
\begin{lstlisting}[frame=single, caption={Pattern wikipedia.fr}, label={lst:wikipedia}]
<div id="mw-navigation">
  <div id="mw-head">...</div>
  <div id="mw-panel">...</div>
</div>
\end{lstlisting}

\paragraph{Règle 7} Les balises de titre sont utilisées pour introduire un nouveau contenu. Cela donne une indication sur l'introduction d'un nouveau contenu.
\begin{lstlisting}[language=HTML, frame=single, caption={pattern eclipse.org/actf}, label={lst:}]
<div class="sideitem">
 <h6>Related links</h6>
  <ul>
  <li><a href="...">Project proposal</a></li> 
  <li><a href="...">Creation Review Slides </a></li>
  <li><a href="...">1.0 Release &amp; Graduation Review</a></li> 
  </ul>
 </div>
\end{lstlisting}
\paragraph{Règle 8} Dans l'exemple \ref{lst:eclipse} l'heuristique va extraire la structure qui correspond au menu grâce à la fonction de distance qui va identifier une distance visuelle. Dans l'exemple ci-dessous le noeud identifié par \emph{header-nav} ne partage pas les mêmes propriétés d'arrière-plan.
\begin{lstlisting}[language=HTML, frame=single, caption={pattern eclipse.fr}, label={lst:eclipse}]
<body>
 <div id="header-menu">
	<div id="header-nav" style="background:..."></div>
	<div id="header-utils"></div>
 </div>
</body>
\end{lstlisting}
\paragraph{Règle 9} Dans l'exemple \ref{lst:legibase}, si les concepteurs respectaient les spécifications de HTML 4, ils auraient annotés le contenu comme une collection logique avec la balise \emph{ul} (indiquant une liste). Pour détecter ce genre de configuration on peut vérifier la taille du plus grand des enfants. Si celui-ci est faible alors on peut en déduire que ce groupe de n\oe{}ud forment une structure logique.
\begin{lstlisting}[language=HTML, frame=single, caption={pattern legibase.htm}, label={lst:legibase}]
<div class="blockMenuGauche taille-relatif=5">
  <div class="menuGaucheInact taille-relatif=5"><a href=""></a></div>
  <div class="menuGaucheInact taille-relatif=5"><a href=""></a></div>
  <div class="menuGaucheInact taille-relatif=5"><a href=""></a></div>
  <div class="menuGaucheInact taille-relatif=5"><a href=""></a></div>
</div>
\end{lstlisting}

\section{Annotation de structures \label{sec:annotation}}
Dans la section \ref{sec:extraction} nous présentons une méthode permettant de partitionner une page. Le résultat est l'obtention d'une structure hiérarchique où chaque noeud correspond à un regroupement de noeuds ayant une sémantique ensemble. Une grande partie des noeuds de cette structure est directement aliénable avec notre méta-modèle. Cependant une partie ne l'est pas, c'est le cas d'une partie des \emph{LANDMARK} : Navigation, Complementary, Banner, Main, ContentInfo. Nous introduisons quelques pistes pour annoter ces éléments.
L'idée de l'approche proposée par \citep{chen2001function} est de décrire suivant une modèle basée sur les fonctionnalités 
nous construisons une fonction détectant les menus de navigation dans une page. 



\paragraph{Navigation}
D'après le modèle \citep{chen2001function} un menu de navigation est un objet composite dont les enfants de la majorité des enfants de la racine sont des objets de navigation. Dans le cas d'un menu de navigation le modèle suffit car les menus de navigation sont construit sur le  pattern, un ensemble de lien navigation. 

\paragraph{Banner} 
En analysant visuellement une bannière on comprend qu'elle donne des renseignements générales sur la page que l'on est en train de consulter. Elle se compose le plus souvent : 
\begin{itemize}
	\item d'un logo qui donne l'identité visuelle du site. 
	\item un titre introduisant le domaine (adresse du site) ou résumant le contenu principal de la page
	\item un menu de navigation qui peut être vu comme la description de l'ossature du site (présente les différentes pages du site web)
	\item un formulaire soit pour recherche un contenu dans le site web ou se connecter à son compte utilisateur.
\end{itemize}

Sur la base de ces éléments on peut décrire, au regard du modèle \citep{chen2001function}, une bannière comme étant une composition d'objets :
\begin{itemize}
	\item d'objets simples de type \emph{Informative} qui peuvent avoir des propriétés des sémantème différentes : une image pour le logo, un titre.
	\item d'objets composites de type \emph{Navigation}
	\item d'objets de type \emph{Interactif} avec une propriété de sémantème formulaire   
\end{itemize}

\paragraph{Complentary}
La méta-classe complementary fournit un contenu complémentaire à la page. Elle correspond à un ensemble de lien de navigation qui pointent sur des pages fournissant une information complémentaire. Le modèle de \citep{chen2001function} ne permet pas de discriminer cette objets dans une page. Il manque une information de localisation. Le menu complementaire à ainsi tendance à ce être localisé en bas de la page.
  
\chapter{Résultats \label{chap:resultat}}
\fp{Je vais mettre des exemples de découpages successives sur plusieurs pages et mettre en avant que le découpage est cohérent avec la sémantique du contenu + mettre en avant l'amélioration avec VIPS car on implémente pas le calcule des séparateurs qui est très coûteux}

\section{Extraction}
\subsection{}
\begin{figure}
\centering
\begin{subfigure}[H]{0.5\textwidth}
	\includegraphics[width=\textwidth]{img/page_berger.png}
	\caption{Avant segmentation}
\end{subfigure}
\begin{subfigure}[H]{0.5\textwidth}
	\includegraphics[width=\textwidth]{img/segmentation_page_berger.png}
	\caption{Après segmentation}
\end{subfigure}
\end{figure}
\subsection{}
\tikzstyle{every node}=[draw=black,thick,anchor=west]
\tikzstyle{selected}=[draw=red,fill=red!30]
\tikzstyle{optional}=[dashed,fill=gray!50]
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
  \node {html}
    child { node {head}}		
    child { node {body}
	    child { node {div}
	    		child { node {div}}}
    };
\end{tikzpicture}

\subsection{?}
\begin{tikzpicture}[
    fact/.style={rectangle, draw=none, rounded corners=1mm, fill=blue, drop shadow,
        text centered, anchor=north, text=white},
    state/.style={circle, draw=none, fill=orange, circular drop shadow,
        text centered, anchor=north, text=white},
    leaf/.style={circle, draw=none, fill=red, circular drop shadow,
        text centered, anchor=north, text=white},
    level distance=0.5cm, growth parent anchor=south
]

\node (State00) [state] {$CO$} [->]
        child{
            node (State01) [state] {$CO$}
            child {  
            		node (State22) [leaf] {$BO$}}
            	child {
            		node (State23) [leaf] {$BO$}}
		}
		child{
            node (State02) [state] {$CO$}
            child {  
            		node (State24) [leaf] {$BO$}}
            	child {
            		node (State25) [leaf] {$BO$}}
		};


\end{tikzpicture}

\chapter{Conclusion \label{chap:conclusion}}

\fp{synthèse du travail effectué et perspectives}

\begin{appendices} 

\section{Méta-modèle de Contenu}
\paragraph{Widget}

Élément graphique dans une page web (bouton, liste déroulant, tableau,
\emph{etc.}). Il peut définir des éléments et un contenu interactif
(\emph{e.g }formulaire d'inscription, bar de recherche, fils d'actualité). 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Role\tabularnewline
\hline 
Sous Classe & Input, Command\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}

\begin{comment}
\paragraph{Composite}

Un élément composite est une composition d'éléments. Il représente un lien
d' agrégation fort entre deux éléments, c'est-à-dire que si l'on supprime
une classe agrégée la classe composite est détruite. Dans un contexte d'accessibilité il va permettre d'organiser la navigation dans les objets.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Widget\tabularnewline
\hline 
Sous Classe & Select\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}
\end{comment}

\paragraph{Input }

L'ensemble des éléments permettant des entrées utilisateurs.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Widget\tabularnewline
\hline 
Sous Classe & Checkbox, Option, Select, Textbox\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Option}

Elément sélectionnable dans une liste.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Input\tabularnewline
\hline 
Sous Classe & Radio\tabularnewline
\hline 
Alignement HTML & <option>\tabularnewline
\hline 
\end{longtable}


\paragraph{Select}

Élément permettant à l'utilisateur de faire des sélections parmi
un ensemble de choix. 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Composite, Group, Input \tabularnewline
\hline 
Sous Classe & Combobox, Listbox, RadioGroup\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Command}

Élément qui exécute des actions mais ne reçoit pas d'informations
en entrée.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Widget\tabularnewline
\hline 
Sous Classe & Button, Link\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Button}

Élément graphique qui déclenche une action. Typiquement un bouton
de validation d'un formulaire.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Command\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <button>\tabularnewline
\hline 
\end{longtable}


\paragraph{Link}

Définit une référence interactive vers une ressource interne ou externe
à la page. Les navigateurs implémentent un comportement de navigation.
Par exemple, une navigation vers une ressource interne peut être dans
une page se déplacer de l'élément d'en-tête à l'élément de pied de
page. Une navigation externe la page peut être un changement de page.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Command\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <a>, <link>\tabularnewline
\hline 
\end{longtable}


\paragraph{Checkbox}

Indique qu'une instance est cochable. Trois valeurs sont possibles
(vraie-faux-mixte), indiquant si l'élément est coché ou non. La valeur
mixte est utilisée dans le contexte d'un groupe d'instance de type
\emph{checkbox.} Par exemple, lorsqu' il y' a au moins un élément
coché et un non coché (\emph{e.g }figure \ref{metaclass-checkbox}).

\begin{longtable}{|l|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Input\tabularnewline
\hline 
Sous Classe & Radiobox\tabularnewline
\hline 
Propriété & checked {[}vraie, faux, mixte{]}\tabularnewline
\hline 
Alignement HTML & <input type= 'checkbox' />\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<input type="checkbox">I have a bike<br> 
<input type="checkbox">I have a car 
\end{lstlisting}
\includegraphics[width=0.5\linewidth]{img/metamodele/checkbox.png}
\caption{Exemple de checkbox\label{metaclass-checkbox}} \end{center} \end{figure}


\paragraph{Radio}

Radio est une instance cochable. Il fait toujours partie d'une liste
d'élément d'au moins deux éléments. Il présente la contrainte que
l'on ne peut sélectionner qu'un seul élément parmi la liste de choix
auquel il appartient (\emph{e.g }figure \ref{metaclasse-radio}).

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Checkbox, Option\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <input type=\textquotedbl{}radio\textquotedbl{}/>\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center}
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<input type="radio">Male<br> 
<input type="radio">Female
\end{lstlisting}
\includegraphics[width=0.5\linewidth]{img/metamodele/radio.png}
\caption{Exemple de radio \label{metaclasse-radio}} \end{center} \end{figure}


\paragraph{RadioGroup}

C'est une collection logique d'élément Radio. Dans html, la collection
logique est exprimée par l'attribut name (\emph{eg }figure \ref{meta-classe_radiogroup})

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Select\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<input type="radio" name="vin">rouge
<input type="radio" name="vin">blanc
<input type="radio" name="vin">rose
\end{lstlisting}
\includegraphics[width=0.5\linewidth]{img/metamodele/radiogroup.png}
\caption{Exemple de radiogroup \label{meta-classe_radiogroup}} \end{center} \end{figure}


\paragraph{Listbox}

Wigdet qui permet de sélectionner un ou plusieurs élément dans une
liste de choix(\emph{e.g }figure \ref{meta-classe_select}). 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & List, Select\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <SELECT>\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<select>   
  <option value="volvo">Volvo</option>   
  <option value="saab">Saab</option>   
  <option value="opel">Opel</option>   
  <option value="audi">Audi</option> 
</select>
\end{lstlisting}
\includegraphics[width=0.25\linewidth]{img/metamodele/select.png}
\caption{Exemple de select \label{meta-classe_select}} \end{center} \end{figure}


\paragraph{Combobox}

Élément qui permet de remplir un champ texte selon des options présentées
dans une liste déroulante.

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Select\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & \emph{cf. }figure \ref{meta-classe_combobox}\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<input type=text list=browsers >
<datalist id=browsers >
   <option> Google
   <option> IE9
   <option> Firefox
</datalist>
\end{lstlisting}
\caption{Exemple de combobox \label{meta-classe_combobox}} \end{center} \end{figure}


\paragraph{Structure}

Éléments de structuration dans une page web. Ce sont l'ensemble des
éléments qui permettent d'organiser le contenu dans une page de manière
logique.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Roletype\tabularnewline
\hline 
Sous Classe & Section, Sectionhead, Separator, Texte\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{List}

Les listes contiennent des éléments dont le rôle est \emph{listitem
}ou des éléments dont le rôle est \emph{group} qui contiennent eux
même des éléments \emph{listitem} (\emph{e.g }figure \ref{metaclass-orderlist}).

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & région\tabularnewline
\hline 
Sous Classe & Listbox, Ol, Ul\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Ul (unorder list)}

Liste non ordonnée d'éléments (\emph{e.g }figure \ref{metaclass-unorderlist}).

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & List\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <ul>\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<ul>
  <li>Cafe</li>
  <li>Thé</li>
  <li>Lait</li> 
</ul>
\end{lstlisting}
\caption{Exemple de liste non-ordonnée \label{metaclass-unorderlist}} \end{center} \end{figure}


\paragraph{OL(order list) }

Liste ordonnée d'éléments

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & List\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <ol>\tabularnewline
\hline 
\end{longtable}

\begin{figure}[htb]
\begin{center} 
\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<ol>
  <li>Cafe</li>
  <li>Thé</li>
  <li>Lait</li>
</ol>
\end{lstlisting}
\includegraphics[width=0.5\linewidth]{img/metamodele/orderlist.png}
\caption{Exemple de liste ordonnée\label{metaclass-orderlist}} \end{center} \end{figure}



\paragraph{Text}

Éléments qui définissent un état logique (sémantique) d'un texte,
en opposition à un état physique (mise en forme). Elle recense donc les éléments
de HTML qui apportent un état logique. On exclut donc les éléments
de mise en forme telles que <b> (bolt) qui traduisent un état physique
(mise en forme) mais seront exprimés dans le méta-modèle de CSS. 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Structure\tabularnewline
\hline 
Sous Classe & Emphase, Abbreviation, Strong, Cite\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}

\newpage


\paragraph{Emphase}

Mise en relief d'une partie du texte. Elle est généralement utilisé
pour mettre en évidence un résumé d'article.


\paragraph{Abbreviation}

Définit une abreviation.

\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.1\textwidth, 
	xrightmargin=.1\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<p>Tony Blair est le premier 
ministre de 
la <abbr title="Grande-Bretagne">GB</abbr></p>  
\end{lstlisting}
\newpage

\paragraph{Strong}

Mot important dans un texte.

\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.1\textwidth, 
	xrightmargin=.1\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
<p>Avant de faire le truc X 
il est <strong>nécessaire</strong> de faire le truc 
Y avant.</p> 
\end{lstlisting}



\paragraph{Cite}

Elément de citation.

\lstset{    
	basicstyle=\footnotesize, 
	frame=tb,   
	xleftmargin=.2\textwidth, 
	xrightmargin=.2\textwidth 
} 
\begin{lstlisting}[frame=single, language=html] 
Ce référerer à la 
norme <cite>[ISO-0000]</cite> 
\end{lstlisting}

\newpage

\paragraph{Section}

Définit une section comme une unité de confinement structurelle
dans une page. On spécifie dans notre méta-modèle que les éléments
héritant de section définissent des limites au contenu qu'il englobe.
Ils fournissent un environnement contextuelle, c'est-à-dire une portée
sémantique aux éléments. Par exemple, les éléments de titre se rapportent
à l'élément de section qui l'a introduit.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline isoler
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Structure\tabularnewline
\hline 
Sous Classe & Group, Region, Paragraph, Tablecell, Listitem\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{ListItem}

Un élément dans une liste. Il est contenu dans une \emph{listitem}.

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <li>\tabularnewline
\hline 
\end{longtable}


\paragraph{Group}

Élément regroupant une collection logique d'éléments (eg. figure \ref{meta-classe_group}).

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & Select\tabularnewline
\hline 
Alignement HTML & <fieldset>\tabularnewline
\hline 
\end{longtable}

%\begin{figure}[htb]
%\begin{center} 
%\lstset{    
	%basicstyle=\footnotesize, 
	%frame=tb,   
	%xleftmargin=.1\textwidth, 
	%xrightmargin=.1\textwidth 
%} 
%\begin{lstlisting}[frame=single, language=html] 
%<form>  
%<fieldset>   
%Nom: <input type="text"><br>   
%Email: <input type="text"><br>   
%Date de naissance: <input type="text">  
%</fieldset> 
%</form> 
%\end{lstlisting}
%\includegraphics[width=1\linewidth]{imgmetamodel/group.png}
%\caption{Exemple de Group \label{meta-classe_group}} \end{center} \end{figure}


\paragraph{Region}

Un groupement thématique dans une page. Éléments d'information sur
une même thématique : représente une section générique d'un document.

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & Article, Landmark, List\tabularnewline
\hline 
Alignement HTML & <section>\tabularnewline
\hline 
\end{longtable}


\paragraph{Article}

Contenu autonome dans un page

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Region\tabularnewline
\hline 
Alignement HTML & <article>\tabularnewline
\hline 
\end{longtable}


\paragraph{Paragraphe}

Élément rajouter au méta-modèle de aria. Définit une composition d'élément
textuelle comme étant un paragraphe dans une page.

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <p>\tabularnewline
\hline 
\end{longtable}

%\begin{figure}[htb]
%\begin{center} 
%\lstset{    
%	basicstyle=\footnotesize, 
%	frame=tb,   
%	xleftmargin=.1\textwidth, 
%	xrightmargin=.1\textwidth 
%} 
%\begin{lstlisting}[frame=single, language=html] 
%<p>Premier paragraphe du texte. 
%J'aime les licornes beaucoup    
%beaucoup beaucoup</p> 
%<p>Deuxième paragraphe du texte. 
%Et si j'en avais une apprivoisée    
%je serais très contente.</p>
%\end{lstlisting}
%\caption{Exemple de Paragraphe \label{meta-classe_group}} \end{center} \end{figure}


\paragraph{Tablecell}

Cellule d'une élément \emph{Table} (\emph{eg. }figure \ref{meta-classe_grid}).

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <td>\tabularnewline
\hline 
\end{longtable}

\newpage

\paragraph{Rowheader}

Une cellule contenant des informations d'entête pour une ligne de
\emph{Table}(\emph{eg. }figure \ref{meta-classe_grid}).

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Section\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <th>\tabularnewline
\hline 
\end{longtable}

\paragraph{Tablerow}

Une ligne dans un \emph{Table}(\emph{eg. }figure \ref{meta-classe_grid}).

\begin{longtable}{|c|c|}
\hline 
Caractéristique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Group\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <tr>\tabularnewline
\hline 
\end{longtable}

\newpage

\paragraph{Table}
Élément qui contient des données tabulaires organisées en ligne et colonne
(\emph{eg. }figure \ref{meta-classe_grid}). 

\begin{longtable}{|c|c|}
\hline 
Caractéritique & Valeur\tabularnewline
\hline 
\endhead
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Region\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <table>\tabularnewline
\hline 
\end{longtable}

%\begin{figure}[htb]
%\begin{center} 
%\lstset{    
%	basicstyle=\footnotesize, 
%	frame=tb,   
%	xleftmargin=.1\textwidth, 
%	xrightmargin=.1\textwidth 
%} 
%\begin{lstlisting}[frame=single, language=html] 
%<table style="width:300px"> 
%<tr>
%	<th>Nom</th>
%	<th>Prenom</th>
%	<th>Age</th> 
%<tr>
%<tr>   
%	<td>Jill</td>   
%	<td>Smith</td>		   
%	<td>50</td>   
%</tr> 
%<tr>   
%	<td>Eve</td>   
%	<td>Jackson</td>		   
%	<td>94</td> 
%</tr> 
%<tr>   
%	<td>John</td>   
%	<td>Doe</td>		   
%	<td>80</td> 
%</tr> 
%</table>
%\end{lstlisting}
%\includegraphics[width=0.5\linewidth]{img/metamodele/table.png}
%\caption{Exemple de Table \label{meta-classe_grid}} \end{center} \end{figure}


\paragraph{Separator}

Élément qui marque une division dans le contenu d'une section. Il permet
de mieux signaler les contenus sémantiquement différents. Ce sont des séparateurs
visuelles (lignes de pixels vides horizontales ou verticales entre deux
éléments). 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Structure\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <hr>\tabularnewline
\hline 
\end{longtable}


\paragraph{SectionHead}

Élément qui résume ou décrive brièvement le sujet introduit par
une section.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Structure\tabularnewline
\hline 
Sous Classe & Heading\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Heading}

Définit un élément titre

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Sectionhead\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <H1,2,3,4,5,6>\tabularnewline
\hline 
\end{longtable}


\paragraph{Landmark}

Éléments structurels courant dans une page web.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Oui\tabularnewline
\hline 
Super Classe & Region\tabularnewline
\hline 
Sous Classe & Banner, Main, Form, Navigation, Complementary, ContentInfo, Application\tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Banner}

Pour faire l'analogie avec l'entête d'un document, on parle de bannière
pour une page web.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <header>\tabularnewline
\hline 
\end{longtable}

\newpage

\paragraph{Application}

Contenu applicatif dans la page. 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & Audio, Video\tabularnewline
\hline 
Alignement HTML & <header>\tabularnewline
\hline 
\end{longtable}


\paragraph{Complementary}

Région d'un document conçut comme étant complémentaire au contenu du document auquel il est associé.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <ASIDE>\tabularnewline
\hline 
\end{longtable}


\paragraph{ContentInfo}

Région d'un document contenant des informations sur celui-ci. Par
exemple le copyrights associé à un document.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Form}

Élément qui contenant une collection d'éléments formant un formulaire.
Les éléments sont généralement une collection de \emph{command, input
}qui permettent une interaction avec l'utilisateur. Les interactions
permettre d'envoyer des informations à un agent en vu d'un traitement (\emph{cf. }figure \ref{meta-classe_form}). 

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <form>\tabularnewline
\hline 
\end{longtable}

%\begin{figure}[htb]
%\begin{center} 
%\lstset{    
%	basicstyle=\footnotesize, 
%	frame=tb,   
%	xleftmargin=.1\textwidth, 
%	xrightmargin=.1\textwidth 
%} 
%\begin{lstlisting}[frame=single, language=html] 
 %<FORM action="http://unsite.com/inscription">     
%<P>
 % <h3>Inscription</h3>
  %<LABEL for="prenom">Prénom : </LABEL>
   %         <INPUT type="text" id="prenom"><BR>
  %<LABEL for="nom">Nom : </LABEL>
   %         <INPUT type="text" id="nom"><BR>
 % <LABEL for="email">e-mail : </LABEL>
  %          <INPUT type="text" id="email"><BR>
  %<INPUT type="radio" name="genre" value="homme">H<BR>
  %<INPUT type="radio" name="genre" value="femme">F<BR>
  %<INPUT type="submit" value="E"> <INPUT type="reset">
  %</P>
 %</FORM>
%\end{lstlisting}
%\includegraphics[width=0.5\linewidth]{img/metamodele/form.png}
%\caption{Exemple de Form \label{meta-classe_form}} \end{center} \end{figure}


\paragraph{Main}

Le contenu principale dans une page.

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & \tabularnewline
\hline 
\end{longtable}


\paragraph{Navigation}

Région contenant une collection de lien navigable vers des ressources
internes ou externes. Par exemple le menu de navigation d'une page
web (\emph{cf.} figure \ref{meta-classe_navigation}).

\begin{longtable}{|c|c|}
\hline 
Caractéristiques & Valeur\tabularnewline
\hline 
\hline 
Abstrait & Non\tabularnewline
\hline 
Super Classe & Landmark\tabularnewline
\hline 
Sous Classe & \tabularnewline
\hline 
Alignement HTML & <nav>\tabularnewline
\hline 
\end{longtable}

%\begin{figure}[htb]
%\begin{center} 
%\lstset{    
%	basicstyle=\footnotesize, 
%	frame=tb,   
%	xleftmargin=.1\textwidth, 
%	xrightmargin=.1\textwidth 
%} 
%\begin{lstlisting}[frame=single, language=html] 
%<nav>
 % <ul>
  %  <li><a href="#">Accueil</a></li>
   % <li><a href="#">À propos</a></li>
    %<li><a href="#">Contact</a></li>
 % </ul>
%</nav>
%\end{lstlisting}
%\caption{Exemple Navigation \label{meta-classe_navigation}} \end{center} \end{figure}

\section{Méta-modèle de mise en forme}


\paragraph{Méta-classe Box}
La méta-classe \emph{Box} décrit les propriétés de positionnement des éléments de contenu.  Les différentes propriétés sont :
\begin{itemize}
\item display : sert à définir le schéma de positionnement appliqué à la boîte. Les deux principaux étant \emph{inline} et \emph{block}. inline positionne les éléments sur la même ligne alors que \emph{block} positionne les éléments les un sous les autres. 
\item margin (top, left, right, bottom) : spécifie l'espacement du bord extérieur de la boîte.
\end{itemize}

\paragraph{Méta-classe Style}
La méta-classe \emph{Style} décrit les boîtes rectangulaires qui sont générées pour les éléments de l'arbre du document et leurs propriétés de positionnement (\emph{cf.}figure \ref{meta-classe_style}).  Les différentes propriétés sont :
\begin{itemize}
	\item padding : l'air d'espacement (\emph{padding})
	\item border-width épaisseur de bordure
	\item border-style : style de la bordure
	\item border-color : la couleur de bordure
	\item border-{[}color, image{]}\emph{ }: arière-plan
\end{itemize}

\paragraph{Méta-classe Text}
La méta-classe \emph{Text} décrit la représentation visuelle des caractères, mots et paragraphes contenu dans une boîte. Les différentes propriétés sont :
\begin{itemize}
	\item text-indent : décrit un alinéa 
	\item text-align : décrit un alignement. Exemple de valeur possible : alignement de texte à gauche, droite, centré, \emph{ect}.
	\item decoration : décrit un trait en-dessous, trait au-dessus, rayure et
clignotement
	\item text-shadow : décrit des effets d'ombrage appliquer au texte
	\item letter-spacing : décrit l'espacement entre les mots
	\item word-spacing : décrit l'espacement entre les mots
	\item text-transform : décrit les effets de capitalisation dans le texte.
Par exemple la valeur \emph{uppercase} définit que les lettres de
chaque mots soient en majuscule, \emph{lowercase} décrit l'inverse.
	\item color : décrit la couleur du texte
\end{itemize}

\paragraph{Méta-classe Police}
La méta-classe \emph{Police} décrit la représentation visuelle des caractères : 
\begin{itemize}
	\item font-family : décrit les noms de famille générique de la police du texte (\emph{e.g new century schoolbook})
	\item font-style : style de la police (\emph{e.g italic})
	\item font-weight : décrit la graisse de la police
	\item font-size : décrit la taille de la police\end{itemize}
\end{appendices} 
\newpage
\printglossaries

\bibliographystyle{plain}
\bibliography{biblio}
\end{document}
